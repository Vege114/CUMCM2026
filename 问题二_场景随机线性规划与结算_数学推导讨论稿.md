# 问题二：场景随机线性规划与实际结算——完整数学推导讨论稿

> 日期：2026-09-12。性质：供组内阅读、讨论和决定的数学方案，不是已实现或已验证的随机规划模型。
>
> 本文只推导模型，不修改预测模型、规划程序或论文。已有 `codex/q2-linear-planning` 分支实现的是点预测两层 LP；下文的历史场景、信息树与随机目标尚未在该分支实现。
>
> 已确定：接收队友预测；采用线性规划方向；不设置充放电互斥约束；取消第三层购电量优化；尽量保留第二层平稳性。往返效率沿用90%。本文不把这些已确定事项重新当作待选方案，但会解释连续松弛的适用边界。
>
> 尚未确定的事项集中在第十五节。各处“建议”只是讨论起点，不表示已获确认，也不表示已完成实验。

## 一、我们负责的任务与整体逻辑

队友负责：在每天0:00，预测当天每个十分钟区间的负载和光伏。

我们负责：

1. 用历史预测与历史实际值构造误差场景；
2. 在所有可能场景下共同选择一份当天购电计划；
3. 在预期费用预算内，进一步改善电池运行平稳性；
4. 当天逐段执行，普通购电计划固定，电池响应已经观测到的供需；
5. 用实际紧急购电量结算，并把真实电池状态传给下一天。

全过程为：

**点预测与历史误差 → 可能的供需路径 → 一份午夜购电计划 → 因果电池控制 → 实际账单。**

这里有两个容易混淆的“层次”：

- **两层目标**：先费用，后平稳性，一共顺序求解两次，不设置第三层。
- **多阶段决策**：午夜先决定购电，日内随着信息逐步出现决定电池动作。决策时点可以很多，并不意味着目标有很多层。

本文的目标是建立“考虑多阶段信息的两层连续随机规划”。随机性通过有限场景近似；场景固定后，待求解的矩阵模型仍然是线性的。

## 二、题意边界与单位

### 2.1 当天可用的信息

每天0:00可以使用：此前实际负载与光伏、此前真正发布的预测、当前电池状态、当天已知分时电价和日历信息。

不能使用：当天尚未发生的实际负载和光伏；附件三的外部光伏预报；附件四的波动电价。

当天144段普通购电量在0:00锁定。日内只能控制电池、处理剩余电量和补足供电缺口，不进行问题三的普通购电上调、下调交易。

### 2.2 功率转成区间电量

将一天离散为

\[
T=144,\qquad \Delta t=\frac16\ \mathrm{h},\qquad t=1,\ldots,T. \tag{1}
\]

其中，\(T\)是每天区间数量，\(\Delta t\)是区间时长，\(t\)是十分钟区间索引。附件00:10表示00:00—00:10这一段的右端时间标签。

队友输出的是功率，进入能量平衡前转换为

\[
\widehat\ell_{d,t}=\widehat L_{d,t}\Delta t,\qquad
\widehat v_{d,t}=\widehat V_{d,t}\Delta t. \tag{2}
\]

其中，\(d\)是日期索引；\(\widehat L_{d,t}\)、\(\widehat V_{d,t}\)分别是当天0:00发布的负载、光伏预测功率，单位kW；\(\widehat\ell_{d,t}\)、\(\widehat v_{d,t}\)是对应区间电量，单位kWh。帽号表示预测，不表示实际。

后续的购电、充电、放电、储电全部使用kWh，不能在储电递推中再次乘以\(\Delta t\)。

### 2.3 固定电池参数

\[
\eta_c=\eta_d=\sqrt{0.9},\qquad
E_{\min}=1200,\quad E_{\max}=10800,\quad
\overline q=P_{\max}\Delta t=\frac{5000}{6}. \tag{3}
\]

其中，\(\eta_c,\eta_d\)是无量纲的充、放电效率，其乘积为往返效率0.9；\(E_{\min},E_{\max}\)是电池内部储电量边界，单位kWh；\(P_{\max}=5000\) kW是单向功率上限；\(\overline q\)是每段最大交流侧充电量或放电量，约833.333 kWh。

题面额定容量12000 kWh与实际允许运行上限10800 kWh不是同一个量。

## 三、为什么不必要求队友重新训练概率预测模型

### 3.1 当前已有的数据

现有 exp003 的 `evaluation_predictions.npz` 保存334日、每天144段、负载与光伏两个通道的点预测。它没有直接给出预测区间、分位数或概率密度。

但是，历史点预测与附件二实际值配对后，可以建立**经验误差分布**。因此，预测负责人不必先改造成概率预测网络。

需要队友提供：预测日期、0:00发布时间、144段负载与光伏值、单位、模型版本和训练截止日期。我们自行配对历史实际值。

注意：现有 `raw_residual`、`residual_42`一类字段表示“神经网络对周期值的修正”，不是“实际值减最终预测值”的预测误差，不能混用。不同随机种子的预测也不能直接当作已经校准的概率场景。

### 3.2 建立严格可见的历史误差库

在日期\(d\)的0:00，定义可用历史集合

\[
\mathcal H_d=\{j:\ j<d,\ \text{日期 }j\text{ 的午夜预测已留档，且全天实际已完成观测}\}. \tag{4}
\]

其中，\(\mathcal H_d\)是可用供体日集合，\(j\)是历史日期。问题二只需要历史午夜预测的全天结果完成，不必等待该日18:00预报所覆盖的次日窗口。

如果要用最近\(H\)天，先按时间截断这个集合。\(H\)为历史窗口长度，是待选参数；不能看完全年成绩再为每一天倒推最佳窗口。

历史误差为

\[
\varepsilon^L_{j,t}=L^{\mathrm{act}}_{j,t}-\widehat L_{j,t},\qquad
\varepsilon^V_{j,t}=V^{\mathrm{act}}_{j,t}-\widehat V_{j,t}. \tag{5}
\]

其中，\(L^{\mathrm{act}}_{j,t},V^{\mathrm{act}}_{j,t}\)是历史实际功率；\(\varepsilon^L_{j,t},\varepsilon^V_{j,t}\)是功率预测误差，单位kW。必须使用历史当时真正可用的预测，不能用后来训练的模型回头拟合后冒充样本外误差。

## 四、从历史误差得到有限概率场景

### 4.1 整日联合抽样

从\(\mathcal H_d\)中选取\(S\)个历史日期\(j_1,\ldots,j_S\)，将每个日期的全天两变量误差整体作为一个样本：

\[
\boldsymbol\varepsilon_s=
\big((\varepsilon^L_{j_s,1},\varepsilon^V_{j_s,1}),\ldots,
(\varepsilon^L_{j_s,T},\varepsilon^V_{j_s,T})\big). \tag{6}
\]

其中，\(s=1,\ldots,S\)是场景索引，\(S\)为选取的场景数，\(\boldsymbol\varepsilon_s\)是一条长度为144、每段包含负载与光伏误差的联合路径。

这样做保留两种关系：

- 同一天负载误差和光伏误差的相关性；
- 前后时段误差的连续性，例如连续一小时高估光伏。

不建议独立抽取每个十分钟点，因为那会破坏电池必须面对的连续缺口结构。

### 4.2 给当前预测添加历史误差

\[
L_{t,s}=\max\{0,\widehat L_{d,t}+\varepsilon^L_{j_s,t}\},\qquad
V_{t,s}=M_{d,t}\max\{0,\widehat V_{d,t}+\varepsilon^V_{j_s,t}\}. \tag{7}
\]

其中，\(L_{t,s},V_{t,s}\)是场景\(s\)下当前日期的可能负载与光伏功率；\(M_{d,t}\in\{0,1\}\)是根据午夜以前资料确定的日照可用掩码，夜间为0。该掩码是已知输入，不是优化的整数变量。不能根据当天未来实际日照生成它。

非负裁剪与夜间处理会改变原始误差统计，因此应保留处理后的场景并核验；不能声称处理前后的均值、方差完全不变。

再转为电量：

\[
\ell_{t,s}=L_{t,s}\Delta t,\qquad
v_{t,s}=V_{t,s}\Delta t,\qquad
n_{t,s}=\ell_{t,s}-v_{t,s}. \tag{8}
\]

其中，\(\ell_{t,s},v_{t,s}\)分别为场景负载、光伏电量，\(n_{t,s}\)为净负荷电量，单位均为kWh。净负荷可以为负，负值意味着光伏富余，不能将其统一裁剪为非负。

### 4.3 概率从哪里来

最简单的经验近似是对选中的代表日赋等权：

\[
\pi_s=\frac1S,\qquad \sum_{s=1}^{S}\pi_s=1. \tag{9}
\]

其中，\(\pi_s\)为场景权重。它是经验概率模型，不是证明真实未来以精确概率\(1/S\)重复该曲线。

若采用聚类压缩场景，权重应使用该代表场景所代表的原始样本权重之和，不应默认所有聚类代表等权。若按相似日期加权，特征只能来自当前午夜已知的信息。

历史误差法隐含“选取的历史误差对当前有参考价值”。季节变化、预测版本变化和极端天气可能破坏这一近似，应在讨论和验证中说明。

## 五、先推导费用目标，再理解为什么要多买一些

### 5.1 每条场景的账单

\[
C_s=\sum_{t=1}^{T}p_tg_t+\sum_{t=1}^{T}5p_te_{t,s}. \tag{10}
\]

其中，\(p_t\)是已知电价，单位元/kWh；\(g_t\)是午夜申报、所有场景共同使用的计划购电量；\(e_{t,s}\)是该场景下的紧急购电量，单位kWh；\(C_s\)为场景总费用，单位元。

\(e_{t,s}\)不是预测团队直接提供的数值，而是由场景供需、购电计划、电池状态及控制规则共同决定。

### 5.2 取经验期望

\[
F_1=\sum_s\pi_sC_s
=\sum_t p_tg_t+\sum_s\pi_s\sum_t5p_te_{t,s}. \tag{11}
\]

其中，\(F_1\)是模型内的预期总购电费用。因为\(\sum_s\pi_s=1\)，计划费只计算一次，而不是随场景数重复收费。

该目标权衡的是：增加一笔确定发生的计划费，是否能减少更大的一笔预期紧急费。

### 5.3 一个不用电池的手算例子

只考虑一个区间，电价1元/kWh，净需求可能为80 kWh或140 kWh，概率分别0.7与0.3：

\[
F(g)=g+5\big[0.7(80-g)^++0.3(140-g)^+\big],\quad g\geq0. \tag{12}
\]

其中，\(g\)为这一段计划购电量，\((a)^+=\max(a,0)\)，\(F(g)\)为预期费用。这里没有电池，紧急量恰好是净需求超过计划的正部。

| 计划量 | 计划费 | 预期紧急费 | 预期总费 |
|---|---:|---:|---:|
| 80 kWh | 80 | 90 | 170 |
| 98 kWh（平均净需求） | 98 | 63 | 161 |
| 140 kWh | 140 | 0 | 140 |

因此，按平均需求买电不一定最便宜。高需求场景虽只有30%概率，五倍紧急单价仍使提前多买划算。

在无电池、需求分布连续且位于内点的简化情形，有

\[
F'(g)=p-5p\Pr(N>g)=0
\quad\Longrightarrow\quad \Pr(N\leq g)=0.8. \tag{13}
\]

其中，\(N\)是随机净需求电量，\(\Pr\)表示概率。离散分布应使用分位数或次梯度条件，不能直接套普通导数；最优量还须满足\(g\geq0\)。这个例子解释费用不对称，并不意味着有电池后逐段使用80%分位数就是全天最优。

## 六、最容易写错的一步：电池不能提前知道自己在哪条未来路径中

### 6.1 为什么“每个场景单独任意调电池”会乐观

如果两条场景上午完全相同，下午一条缺电、一条富余，而优化器上午就对两条场景安排不同放电量，实际上已经提前知道了下午的情况。

只规定各场景共用\(g_t\)，并不足以阻止电池偷看未来。未来尚不可区分时，电池动作也必须共享。

不加这一约束而得到的自由场景调度，可作为相应费用模型的**完全信息松弛下界**，但不能当作实际可执行策略。下界比较须保持相同物理约束、费用定义和终端条件。

### 6.2 当前区间的观测时点也要明确

本文主推导采用：控制器可以根据当前区间内的计量反馈响应当前负载、光伏，但看不到以后区间。十分钟平均值是该反馈过程的离散近似。

这不是声称在区间开始前就知道整个十分钟实际平均值。若团队希望采用“本段开始前决策，只能看上一段计量”的时序，需要把下文信息前缀从包含\(t\)改为只到\(t-1\)，并单独设置本段平衡补救。两种时序不能混用。

### 6.3 非预见性约束

设\(\mathcal I_{t,s}\)为场景\(s\)在决定第\(t\)段动作时已经观察到的信息。若两个场景的可见信息相同，则要求

\[
\mathcal I_{t,s}=\mathcal I_{t,s'}
\Longrightarrow
c_{t,s}=c_{t,s'},\quad d_{t,s}=d_{t,s'}. \tag{14}
\]

其中，\(c_{t,s},d_{t,s}\)分别为场景内交流侧充、放电量。输入供需、状态与动作相同时，补购和剩余电量也应按一致规则处理，不能额外依靠未来场景编号区别对待。

直接对有限历史路径做“前缀完全相同”判断仍可能过于乐观：连续值几乎不会完全相同，第一段就可能唯一识别某个供体日，等于在离散模型中知道了余下一天。

因此，正式模型建议使用**明确规定信息分辨率的信息树**，而不是把每条历史样本的身份当作现实中能够观察到的信息。

## 七、用信息树把因果策略写成线性规划

### 7.1 树的结构与数据

令\(\mathcal N_t\)为第\(t\)段可能到达的信息节点集合，\(\mathcal N=\bigcup_t\mathcal N_t\)。根节点0表示午夜、尚未观察当天供需。

每个非根节点\(n\)包括：

| 符号 | 含义 |
|---|---|
| \(t(n)\) | 节点所对应的十分钟区间 |
| \(a(n)\) | 节点的唯一父节点 |
| \(q_n\) | 从午夜出发到达该节点的无条件概率 |
| \(\ell_n,v_n\) | 该节点代表的当段负载和光伏电量 |
| \(\operatorname{ch}(n)\) | 节点的下一段子节点集合 |

节点概率满足

\[
q_0=1,\qquad \sum_{m\in\operatorname{ch}(n)}q_m=q_n,
\qquad \sum_{n\in\mathcal N_t}q_n=1. \tag{15}
\]

其中，\(q_m/q_n\)是从节点\(n\)到子节点\(m\)的条件概率，且只在\(q_n>0\)时定义；零权重节点可删除。

### 7.2 信息树如何由历史路径构造

一种待讨论的做法是顺序聚类：在同一父节点下，根据当时已可见的供需前缀，把相近观测归为同一类；以该类当前供需的加权平均值形成代表节点，并继承条件权重。

必须满足：

1. 节点划分只能使用相应时刻已可见的信息；
2. 后续只能细分，不把不同历史状态任意重新合并；
3. 同一节点有一组确定的代表供需值；
4. 节点数、分辨率及分支规则在评价前确定；
5. 不能根据真实供体日编号识别未来。

**代表节点会近似原始历史路径分布。** 不应一边合并当前供需不同的场景，一边仍保留各自不同的当前供需，却强迫完全相同的补救变量；这样模型含义会矛盾。采用节点代表值后，费用期望是相对于近似信息树的期望，必须如实标注。

树太粗会丢失极端连续缺口；树太细会增加规模，并使有限样本的未来几乎被提前识别。如何控制分支是待决定问题，不在本文预设已有最优方案。

### 7.3 决策变量

在午夜只设置一组\(g_1,\ldots,g_T\)。对每个节点\(n\)设置：

- \(c_n,d_n\)：该节点的交流侧充、放电量；
- \(e_n,w_n\)：紧急购电与未利用电量；
- \(E_n^-,E_n^+\)：该段开始、结束时电池内部储电量；
- \(P_n\)：该段交流侧净充电功率；
- \(\xi_n\)：相对父节点功率变化的绝对值上界。

所有变量均连续。节点本身已经共享动作，不需要再对同节点的场景重复写式（14）。

### 7.4 节点能量平衡

\[
g_{t(n)}+v_n+d_n+e_n=\ell_n+c_n+w_n,
\qquad g_t,c_n,d_n,e_n,w_n\geq0. \tag{16}
\]

其中，\(g_{t(n)}\)取同一份午夜计划中对应时段的分量；\(e_n,w_n\)允许模型在场景发生偏差时补电或保留未利用量。所有项单位均为kWh。

这一公式允许紧急电参与整个系统平衡，未单独禁止其用于充电。若将题意严格解释为“紧急电只能弥补负载缺口，不能用于储能”，需要额外的控制规则；这一争议见第十二节，不能隐瞒在公式里。

### 7.5 节点储电递推与跨分支状态

\[
E_n^+=E_n^-+\eta_c c_n-\frac{d_n}{\eta_d},
\qquad
E_n^-=\begin{cases}
E_d^{\mathrm{init}},&t(n)=1,\\
E_{a(n)}^+,&t(n)>1.
\end{cases} \tag{17}
\]

其中，\(E_d^{\mathrm{init}}\)是当天午夜真实储电量。父节点之后不同子节点都从父节点执行后的同一储电量出发，不能任意挑选各自有利的初始电量。

同时施加

\[
E_{\min}\leq E_n^-,E_n^+\leq E_{\max},\qquad
0\leq c_n,d_n\leq\overline q. \tag{18}
\]

其中，储电上下界与单段功率对应电量上限已在式（3）定义。日末只保留这些物理边界，暂不添加每日初末相等或终端奖励。

### 7.6 净功率与运行连续性

\[
P_n=\frac{c_n-d_n}{\Delta t},\qquad
P_{\mathrm{prev}(n)}=
\begin{cases}
P_d^{\mathrm{init}},&t(n)=1,\\
P_{a(n)},&t(n)>1.
\end{cases} \tag{19}
\]

其中，\(P_d^{\mathrm{init}}\)为前一天最后一段实际净充电功率，单位kW；\(P_{\mathrm{prev}(n)}\)表示该节点之前已执行的功率。

若保留问题一的功率变化上限，则

\[
-R\leq P_n-P_{\mathrm{prev}(n)}\leq R. \tag{20}
\]

其中，\(R=1000\) kW/相邻十分钟区间是此前运行偏好，不是题面给定硬件额定值。它也可能与极端状态产生冲突，因此是否作为全天所有情形都不可违反的硬约束，仍需讨论。

## 八、第二层的线性化与完整两层模型

### 8.1 用线性不等式表达功率总变差

\[
\xi_n\geq P_n-P_{\mathrm{prev}(n)},\qquad
\xi_n\geq P_{\mathrm{prev}(n)}-P_n,\qquad \xi_n\geq0. \tag{21}
\]

其中，\(\xi_n\)单位为kW。在第二层赋正权重并最小化时，它会取到功率差绝对值，因此无需非线性绝对值求解器。

定义预期归一化总变差

\[
F_2^{\mathrm{TV}}=
\frac{1}{2P_{\max}T}\sum_{n\in\mathcal N}q_n\xi_n. \tag{22}
\]

其中，\(F_2^{\mathrm{TV}}\)是无量纲的预期运行不平稳程度。因为每层节点概率和为1，这个式子相当于按路径概率求全天总变差期望，不是把互斥发生的不同节点全额相加。

它不能等同于电池寿命、退化费用，也不能保证每条极端路径都同样平稳。

### 8.2 可选的周转惩罚，必须明确它不是第三层

如果希望抑制无意义充放电，可以在第二层内部考虑

\[
F_2=F_2^{\mathrm{TV}}+
\beta\frac{\sum_{n\in\mathcal N}q_n(c_n+d_n)}{2P_{\max}\Delta t\,T},
\qquad \beta\geq0. \tag{23}
\]

其中，\(\beta\)为无量纲周转权重，第二项为归一化充放电周转量。\(\beta=0\)就是仅优化总变差。这个权重不是已知电池退化参数，也不能保证完全互斥。

现有点预测代码采用过0.001作为小权重，但不能据此认定它已适用于随机模型。本文建议将是否使用及取值交由讨论决定，不暗中增加新的层次。

启动次数、最短运行/关闭时间、短段缺额涉及离散逻辑，不能完整保留在纯连续LP中。这里的“尽量保留第二层”具体指保留功率平稳性，而不是声称启动次数已被精确优化。

### 8.3 第一层：预期费用最小

令\(\mathcal X\)为式（16）—（21）及信息树状态关系组成的连续可行域，求

\[
\begin{aligned}
C^\star=\min_{x\in\mathcal X}\quad
F_1(x)
&=\sum_{t=1}^{T}p_tg_t
+\sum_{n\in\mathcal N}q_n5p_{t(n)}e_n.
\end{aligned} \tag{24}
\]

其中，\(x\)是所有计划及节点决策变量组成的向量，\(C^\star\)是同一可行域下最小的经验预期费用，单位元。它不是已经实现的真实日费，更不是全年全局最优费用。

### 8.4 第二层：在费用预算内改善平稳性

\[
\begin{aligned}
\min_{x\in\mathcal X}\quad &F_2(x),\\
\mathrm{s.t.}\quad &F_1(x)\leq (1+\delta)C^\star+\epsilon_C.
\end{aligned} \tag{25}
\]

其中，\(\delta\)为费用让步比例，讨论起点沿用0.001；\(\epsilon_C\)为费用数值容差，讨论起点沿用\(10^{-4}\)元。它们约束的是**这个信息树中的预期总购电费用**，不是逐场景最大费用，也不是实际全年费用。

到这里求解结束，不再追加最小购电量，也不再添加“保留第二层后优化别的指标”的第三次求解。

### 8.5 为什么仍然是线性规划

构造完场景和信息树后，\(q_n,p_t,\ell_n,v_n,\eta_c,\eta_d,R\)均为固定输入；目标与约束只包含常数乘变量及线性求和；没有变量乘变量；绝对值已线性化；没有0—1决策。

因此可写成标准形式

\[
\min f^\mathsf T x,\qquad
A_{\mathrm{eq}}x=b_{\mathrm{eq}},\quad
A_{\mathrm{ub}}x\leq b_{\mathrm{ub}},\quad
\underline x\leq x\leq\overline x. \tag{26}
\]

其中，\(f\)为目标系数向量，\(A_{\mathrm{eq}},A_{\mathrm{ub}}\)为等式、不等式系数矩阵，\(b_{\mathrm{eq}},b_{\mathrm{ub}}\)为右端常数，\(\underline x,\overline x\)为变量边界。第二层只需替换目标并增加一条费用预算约束。

## 九、为什么不能把最优场景轨迹直接当作真实执行轨迹

历史构造的代表节点通常不会与真实观测完全相同。即使午夜解出了整棵树，也不能事后挑一条最接近真实未来的叶子，然后把那条叶子的全天电池动作作为“实际执行”。这会利用未来信息。

建议采用在线滚动重求解：实际到达每一段时固定既有购电计划，更新当前状态与可见信息，只执行新解的当前动作。

要准确解释其含义：午夜信息树中的费用是“模型内策略”的费用估计；日内重求解可能改变未来策略，因此不是保证严格兑现午夜整棵树的承诺。预期与实际偏差必须用连续回放评价。

## 十、日内滚动执行模型的推导

### 10.1 固定已经申报的计划

记午夜第二层最终选出的计划为

\[
g_t=\overline g_t,\qquad t=1,\ldots,T. \tag{27}
\]

其中，\(\overline g_t\)表示已经承诺的普通购电量，横线不表示上界。日内不再把它当可调整的普通购电决策。

### 10.2 根据已观察到的前缀更新剩余场景

到第\(k\)段时，只能使用实际前缀和此前历史。一个可选的条件权重形式是

\[
\widetilde\pi_{s\mid k}
=\frac{\pi_s\exp[-D_{s,k}/(2h^2)]}
{\sum_r\pi_r\exp[-D_{r,k}/(2h^2)]}. \tag{28}
\]

其中，\(\widetilde\pi_{s\mid k}\)为观察前缀后的经验场景权重；\(D_{s,k}\)是历史误差前缀与当天已观察误差前缀的标准化距离，不能包含\(k\)之后数据；\(h>0\)为预先选定的匹配尺度。标准化尺度也必须来自历史。

这只是可讨论的经验更新，不是已证明正确的贝叶斯后验。实际应用应处理权重极度集中、数值下溢和少样本问题；可先使用不更新权重的简单基线。更改预测均值时，也应同步处理误差定义，不能把原始残差无说明地加到不同语义的预测上。

当前段用实际观测作为根部确定供需；后续段由剩余场景构造条件信息树。真实储电量与上一段功率替换午夜模型的预测状态。

### 10.3 执行第一层：最小化剩余紧急费用

\[
J_k^\star=\min_{y\in\mathcal X_k(\overline g)}
J_k(y),\qquad
J_k(y)=\sum_{n\in\mathcal N^{(k)}}q_n^{(k)}5p_{t(n)}e_n. \tag{29}
\]

其中，\(k\)是当前区间；\(\mathcal N^{(k)}\)是条件剩余树；\(q_n^{(k)}\)是给定当前信息后的节点概率；\(y\)为剩余节点控制变量；\(\mathcal X_k(\overline g)\)为固定普通购电计划、当前实际状态后的可行域；\(J_k\)为剩余预期紧急购电费。

计划费已经确定，加入执行目标只是增加一个常数，故可略去，但最终账单仍必须计入。

### 10.4 执行第二层：剩余平稳性优化

\[
\min_{y\in\mathcal X_k(\overline g)}F_{2,k}(y),\qquad
J_k(y)\leq(1+\delta_{\mathrm{exec}})J_k^\star+\epsilon_C. \tag{30}
\]

其中，\(F_{2,k}\)是剩余窗口的运行质量指标，按式（22）或（23）将归一化长度改为\(T-k+1\)；\(\delta_{\mathrm{exec}}\)为执行阶段局部费用让步比例，是否与午夜\(\delta\)相同需决定。

只实施当前段动作，下一段再按新状态重求解。不得把每次局部让步简单累计成一个全年0.1%的保证，也不能把上一次已放宽的解再乘1.001当成新的第一层最优值。

### 10.5 跨日传递

\[
E_{d+1}^{\mathrm{init}}=E_{d,T+1}^{\mathrm{act}},\qquad
P_{d+1}^{\mathrm{init}}=P_{d,T}^{\mathrm{act}}. \tag{31}
\]

其中，上标\(\mathrm{act}\)表示实际执行值。次日接收当天实际末储电量与末段功率，不接收午夜预测的末状态。当前纯LP没有最短时长承诺，故不需要虚构相应整数模式状态。

## 十一、去掉充放电互斥：可以省掉整数变量，但必须说明数学边界

### 11.1 无额外运行限制时，常能找到不同时充放电的等价方案

对任一时段，令电池内部净增加量为

\[
u=\eta_c c-\frac d{\eta_d}. \tag{32}
\]

其中，\(c,d\)为原始同时充、放电量，\(u\)为内部净储能变化，单位kWh。

可以构造保持\(u\)不变的单向动作：

\[
(c',d')=
\begin{cases}
(u/\eta_c,0),&u\geq0,\\
(0,-\eta_d u),&u<0.
\end{cases} \tag{33}
\]

其中，\(c',d'\)是替代后的充放电量。它们不同时为正，分别不大于原有对应单向电量，且保持该段末储电量不变。

进一步有

\[
(c-d)-(c'-d')=
\begin{cases}
d\big(1/(\eta_c\eta_d)-1\big),&u\geq0,\\
c(1-\eta_c\eta_d),&u<0,
\end{cases}
\quad\geq0. \tag{34}
\]

其中，左边表示消除循环后减少的交流侧净用电。若允许无上限未利用量，可相应增加\(w\)，保持购电和紧急量不变。因此，在没有其他受影响约束时，可以构造费用相同的单向动作。

但这支持的是“存在某个可替代方案”，不是“求解器返回的每个最优解都自动互斥”。

### 11.2 为什么加入第二层后不能直接套上述结论

式（33）会改变净交流功率，从而改变相邻功率差和总变差。它可能违反硬爬坡约束，也可能破坏第二层最优性。

因此，不能在优化后直接把同时充放电量相减，再声称所有约束、SOC和目标都保持原样。简单相减还会改变内部损耗；保持SOC的正确变换是式（33），但即使使用它，也仍须重新核对功率约束。

### 11.3 一个具体边界例子

电池已满10800 kWh，上一段净充电1100 kW，相邻变化上限1000 kW。当段至少还要净充电100 kW。

真实单向电池在满电时无法继续充电；但连续松弛可以通过同时充、放电消耗能量，在保持净充电功率的同时不超过储电上限。这种松弛解可能存在，而物理动作不可执行。

本稿仍遵守“不加互斥”的决定。建议将每个节点的重叠量记为

\[
o_n=\min(c_n,d_n). \tag{35}
\]

其中，\(o_n\)是事后诊断量，单位kWh，不是新增的优化非线性约束。检查应覆盖午夜场景轨迹和实际执行，不能只检查最终一天总量。

发现明显重叠时，应标记“松弛存在物理偏差”，由团队决定如何处理硬爬坡、第二层或控制器；不能静默恢复整数互斥，也不能把存在重叠的模型费用直接宣传成可实现节约。

## 十二、另一项必须决定的题意解释：紧急电能否用于充电

式（16）允许\(e_n>0\)和\(c_n>0\)同时出现。如果当前普通电价低、未来紧急电价更高，模型可能主动用当前紧急购电给电池充电，留到后面放出。

“紧急电价五倍，所以肯定不会给电池充电”不一定成立：当前五倍低价仍可能低于未来五倍高价，扣除损耗后仍有跨时段收益。

可区分两种解释：

1. **系统平衡补购解释**：紧急电作为额外高价电源，允许服务总平衡，包括正在执行的电池充电。主推导的连续LP采用这一宽松数学形式，但需要团队确认符合题意。
2. **严格负载缺口解释**：只有计划供电、光伏和允许的电池放电不足以满足负载时才紧急购买，不用紧急电主动充电。此时不能仅靠式（16）宣称严格落实，需要额外因果执行规则或更受限策略类。

例如直接要求“充电时紧急量为零”属于互补逻辑，不是一般连续LP约束。不能为满足“纯LP”而暗中把这一物理/交易解释省略。

本事项与“是否设置充放电互斥”不是同一个问题。它影响紧急购电的权限，建议在编码前明确；暂不把其中任何一个选择伪称为题面唯一无争议解释。

## 十三、最终结算与题目要求的输出

### 13.1 真实区间账单

\[
C_{d,t}^{\mathrm{plan}}=p_t\overline g_{d,t},\qquad
C_{d,t}^{\mathrm{emg}}=5p_te_{d,t}^{\mathrm{act}}. \tag{36}
\]

其中，\(C_{d,t}^{\mathrm{plan}}\)、\(C_{d,t}^{\mathrm{emg}}\)分别是实际应支付的计划费和紧急费，单位元；\(e_{d,t}^{\mathrm{act}}\)是实际执行产生的紧急量，不能用场景预期紧急量替代。

每日和评价期总费为

\[
C_d^{\mathrm{act}}=\sum_t\big(C_{d,t}^{\mathrm{plan}}+C_{d,t}^{\mathrm{emg}}\big),\qquad
C_{\mathrm{eval}}=\sum_{d\in\mathcal D_{\mathrm{eval}}}C_d^{\mathrm{act}}. \tag{37}
\]

其中，\(\mathcal D_{\mathrm{eval}}\)为2025-02-01至12-31的334日正式评价集合；一月用于预热、训练和校准，单独记录，不计入该总费。

### 13.2 结算时不能混入的项目

- 没有问题三的普通计划上调、下调费用；
- 已申报但未用完的计划电量仍然收费；
- 未利用光伏不产生售电收入；
- 平稳性目标、周转惩罚和假设的终端储能价值不是电费；
- 不对同一天在多个工作表里重复展示的总费重复求和。

### 13.3 完整输出内容

最终 `result2.xlsx`应包括：每天0:00确定的144段计划购电量、实际四小时充放电汇总、0:00与24:00真实储电量、实际紧急购电区间及电量。

论文指定日期为3月20日、6月21日、9月23日、12月21日，按题目表1—表3展示。

另保存可审计记录：预测版本、场景来源日、节点概率、求解状态、两层目标、节点/实际重叠量、能量与SOC残差、实际功率总变差、最终状态。它们用于证明结果可信，不把所有记录都充当题目主要绩效指标。

## 十四、在编码以前，如何判断推导是否自洽

### 14.1 必须能解释清楚的四条因果关系

1. 当前午夜的场景只来自当时可用历史；
2. 所有可能未来共用一份普通购电计划；
3. 电池当前动作只依赖当时已揭示的信息和真实状态；
4. 实际账单只由已锁定计划和实际补购组成。

### 14.2 后续需要开展的检验，不是本稿声称已经完成的检验

| 检验 | 要排除的问题 |
|---|---|
| 单段无电池手算 | 五倍紧急电价、场景概率或计划费重复计算错误 |
| 小型两分支信息树 | 电池在分支信息出现前提前采用不同动作 |
| 改动未来实际值 | 午夜计划、此前执行动作意外变化 |
| 满电、空电、连续盈余与骤增负载 | 互斥松弛偏差及爬坡约束不可行 |
| 连续两天及跨月回放 | SOC、上一段功率重置或预测版本泄露 |
| 点预测与场景模型同口径比较 | 将预测、控制、预热差异误归因于随机规划 |
| 各层最优性与残差检查 | 将超时可行解称作第一层最优值 |
| 实际轨迹独立结算 | 将模型内预期费用当作最终真实费用 |

第一层若没有取得可信最优状态，不能把一个未知质量的可行值写作\(C^\star\)并宣称严格相对最优的0.1%保证。

### 14.3 本稿建议的最小实验顺序

先冻结预测、场景、初始状态及结算权限。用一个小信息树核对因果性，再测试两层LP的边界。之后才扩大到连续多日和正式334日回放。

对照至少保留：点预测两层LP、场景两层LP。若同时更换日内控制，还应单独比较控制器，以免把全部降费归于场景。

所有正式比较应使用统一的一月共同预热和相同2月1日真实初始状态，或者明确比较整个全年策略连同预热的效果，不能混称。此前已经看过全年结果，后续仍应标注为回顾性时序评价。

## 十五、需要你决定的争议点

下面没有重新询问是否加互斥、是否恢复第三层：这两项已有明确决定。待决定的是如何在这些要求下形成完整、可解释的模型。

### D1：场景中的电池控制怎样保证不偷看未来？

- **方案A：信息树＋日内滚动重求解。** 本稿给出了这条路线的数学骨架。更灵活，但需要决定树的分辨率，午夜期望与真实重求解表现可能不同。
- **方案B：午夜确定共同的电池参考轨迹，配合明确的受限反馈规则。** 更容易解释，但反馈规则必须能被线性描述，或明确其与规划的近似关系。
- **方案C：各场景自由调电池。** 只作为完全信息松弛诊断，不作为正式可执行方法。

**建议：A作为正式推导方向，C只作诊断。不能把“所有场景同一购电计划”误当成已经解决电池非预见性。**

### D2：第二层具体保留什么？

- **A：只保留预期净功率总变差，\(\beta=0\)。** 最接近“平稳性优先”的直观含义。
- **B：总变差＋小权重充放电周转量。** 有助于减少无意义循环，但多一个参数，也不保证互斥。

**建议：先A作为主对照，B作为单因素检验；不要将现有代码的0.001直接视为已确定正式值。**

### D3：1000 kW功率变化上限是硬约束，还是可付出平稳性代价的偏好？

- **A：保持硬限制。** 与问题一设置一致，但遇到满电且前段仍充电等状态可能无法物理执行。
- **B：只在第二层惩罚功率变化，不设硬上限。** 保留连续LP与柔性平滑，但改变了问题一的这项运行偏好。
- **C：通常硬限制，明确应急例外。** 需要规定触发条件和优先级，不能自动无限松弛。

**建议：保留A做基线边界检验；如果实际失败，再由你决定B或C。本文不自行取消既有上限。**

### D4：出现同时充放电的松弛解时怎么办？

- **A：记录并停止认证该方案为物理可执行结果，检查原因后再决定修改。**
- **B：预先设计一个不含整数变量的单向执行规则，并核对规划与执行偏差。** 这会限制策略类，需要额外推导，不能保证与自由LP等价。

**建议：A。不偷偷加回互斥约束，也不直接净化后宣称原目标与约束都不变。**

### D5：紧急购电可否为储能充电？

- **A：作为系统平衡的高价补充电源。** 与式（16）的连续LP一致，但可能主动提前补电储存。
- **B：只补负载缺口，不主动给储能充电。** 更接近通常的“紧急保供”表述，但需要受限策略或额外逻辑，不能声称现有自由LP已经实现。

**建议：由你们先明确题意解释。这项会直接改变数学可行域，不能仅在最后结算时改口径。**

### D6：历史窗口、场景数与权重怎样选？

- **简单起点：** 最近最多56个完整历史日；选取不超过16个代表路径；未聚类抽样时先等权。
- **扩展：** 聚类代表赋簇权重，或按近期/相似程度加权。

**建议：简单起点只作为候选，不预设最优。首月不足时仅使用已有历史；一月预测残差来源与后续预测版本差异需披露。**

### D7：日末储电量怎样处理？

- **A：只保留物理上下界，真实SOC跨日连续。** 最少新增假设，但可能短视。
- **B：历史校准的终端储能价值。** 可体现明日用途，但必须保持实际账单独立。
- **C：更长预测窗口，只申报今天。** 增加预测接口与优化规模。

**建议：先A，不增加每天首尾相等；在其他部分稳定后单独比较B或C。**

### D8：费用让步及预期平稳性如何解释？

- 午夜可暂以\(\delta=0.001\)为讨论起点；执行阶段是否同样取0.001需明确。
- 主稿采用预期平稳性，不等价于每个场景都平稳；若要防止个别场景很差，可另外设每路径指标上限或采用最大值目标，但这是新的设计选择。
- 保留局部费用预算，不能宣称实际全年费用严格只增加0.1%。

**建议：先使用预期总变差，并明确局部预算语义；不立即叠加CVaR等风险目标。**

### D9：当前十分钟的供需何时对控制器可见？

- **A：区间内实时计量反馈的十分钟平均近似。** 本稿采用，允许对当前已发生供需即时响应。
- **B：区间开始前下指令，只使用此前区间计量。** 更严格的离散开环动作时序，需要重新明确当前段应急补救。

**建议：A，但论文必须说明是反馈控制离散近似，不是提前知道当前十分钟所有实际值。**

## 十六、你阅读后优先决定哪几项

最影响数学模型、应先决定的是 **D1（因果控制结构）、D3（硬爬坡）、D5（紧急购电用途）**。D2决定第二层具体形式；其余可以先采用明确记录的简单基线。

确认这些之后，才能把式（16）—（25）和式（27）—（30）准确转为代码。本文没有默认你已经同意这些建议，也没有以推导稿代替真实回放验证。

## 本稿依据与已读范围

- 原题 `C题/C题.md` 中的问题二、储能参数及result2输出要求。
- `reports/discussions/q2-improvement/modules.md` 中的数据、误差路径、购电、控制与结算讨论。
- `experiments/problem2/exp003/predict.py` 与 `data/results/exp003/evaluation_predictions.npz` 的现有点预测接口。
- `experiments/common/neural_v2/scenarios.py` 的历史联合误差构造思路；本文不直接照搬其跨问信息树。
- `experiments/problem2/linear_planning/README.md` 中当前点预测LP的实现边界与互斥反例。

上述文件是理解既有成果的依据。文件中提出的旧计划、建议和未完成工作不等于本次已授权实施；本次交付仅为本 Markdown 数学推导稿。
