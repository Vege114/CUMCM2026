% !TeX root = ../数模通用模板.tex
\section{问题四的模型建立与求解}
\label{sec:q4}

问题四将电价纳入滚动预测。本文采用加权岭回归刻画价格的周期性，并在无日内预报情形中引入供需预测特征；结合小时模式情景规划与日内滚动线性规划，分别制定两类购电策略。决策仅利用当时可得信息，按实际电价结算，并从费用与储能运行强度两方面评价结果。

\subsection{问题四模型的建立}

\textbf{信息边界与电价预测。}
将无日内预报、有日内预报的方案分别记为4-2、4-3。两者均以单HGB、近28日岭校准及前一日负载偏差半量修正作为午夜供需预测；方案4-3再利用已发布光伏预报与已观察负载更新剩余时段。电池参数、十分钟离散和能量平衡沿用前文。附件4的未来电价视为尚未揭示，仅历史实况进入拟合；当期实际电价用于最终结算。

对发布时刻$o$之后的目标槽$i$，价格特征由截距、附件1基准价格、前一日与前一周同槽价格及日内一、二阶谐波组成。方案4-2另加入当时发布的负载、光伏预测值，均换算为MW；历史训练行保留其自身午夜发布的预测，不以事后实测值替代。采用近28日、14日半衰期的加权先验岭回归：
\begin{equation}\label{eq:q4-price}
 \begin{aligned}
 \widehat{\boldsymbol\beta}_o
 &=\arg\min_{\boldsymbol\beta}
 \left\{\sum_{j\in\mathcal H_o}\omega_j
 (p_j-\boldsymbol x_j^{\mathsf T}\boldsymbol\beta)^2
 +(\boldsymbol\beta-\boldsymbol\beta_0)^{\mathsf T}
 \Lambda(\boldsymbol\beta-\boldsymbol\beta_0)\right\},\\
 \widehat p_{i\mid o}&=\max\{10^{-4},\boldsymbol x_{i\mid o}^{\mathsf T}\widehat{\boldsymbol\beta}_o\},
 \qquad \omega_j=2^{-(o-1-j)/(14\times144)}.
 \end{aligned}
\end{equation}
其中$\mathcal H_o$仅含发布前已实现的价格标签；$\boldsymbol\beta_0$的日、周滞后系数各为0.5，其余为零；$\Lambda=\operatorname{diag}(10^{-6},5,\ldots,5)$。方案4-3在各发布时刻重估八维价格模型，不加入额外供需特征。两种调度均使用价格点预测，不另构造随机价格场景。

\textbf{无日内预报：小时模式情景规划。}
方案4-2在午夜确定全天购电量。由近期完整净需求误差路径构造情景，等距选取三条用于混合整数规划；每小时六槽共享一个充放电方向$z_h$。记$B=5000/6$\,kWh，除供需平衡、SOC递推及安全边界外，对每条情景$s$施加
\begin{equation}\label{eq:q4-modes}
 \begin{aligned}
 0\le c_{s,t}&\le Bz_{h(t)},&
 0\le d_{s,t}&\le B(1-z_{h(t)}),\\
 c_{s,t}&\le Bu_{s,t},&
 e_{s,t}&\le[n_{s,t}]_+(1-u_{s,t}),\qquad z_h,u_{s,t}\in\{0,1\}.
 \end{aligned}
\end{equation}
这里$n_{s,t}$为情景净需求电量。以预测购电费、5倍短缺代价、方向切换和吞吐惩罚之和为目标，切换权重取50元/次，吞吐权重取0.002元/kWh；普通日末库存按最低预测电价除以单向效率计值。获得小时模式后，固定该模式，利用最近至多28条完整误差路径，以实际反馈规则回放情景并采用L-BFGS-B修正购电量。该步普通日末库存价值取0.45元/kWh，评价末日均置零。购电量一经确定，当天不再调整。

\textbf{有日内预报：保守上调滚动规划。}
方案4-3在0、6、12、18时利用同发布时间的近期净需求误差路径，等距选取至多七条，求解剩余时段的情景线性规划。日内采用保守上调策略，常规购电量满足
\begin{equation}\label{eq:q4-upward}
 q_{d,t\mid\tau}\ge g_{d,t},\qquad
 t>6\tau,\quad \tau\in\{6,12,18\},
\end{equation}
其中$g_{d,t}$为午夜原计划。后一次发布可覆盖尚未执行的上调安排，下界始终是午夜原计划；已执行槽保持不变。

规划目标采用预测价格下的原计划或1.5倍上调代价，并加入情景短缺、吞吐、功率总变差与日末库存价值。下一次发布前的短缺权重为5，其后取1.5，表示尚有调整机会；吞吐和功率总变差权重分别为0.005元/kWh、0.001元/kW。各情景采用能量平衡、SOC及功率界构成的线性规划近似。实际执行时按当槽净盈余充电、净缺口放电，剩余缺口才紧急购电，充电死区为20\,kWh；真实SOC连续传递至下一发布时刻及次日。

\textbf{费用结算。}
两方案统一按已实现电价$p_{d,t}$结算。在$q_{d,t}\ge g_{d,t}$的上调策略下，总费用为
\begin{equation}\label{eq:q4-cost}
 C=\sum_{d,t}p_{d,t}\left(g_{d,t}+1.5(q_{d,t}-g_{d,t})+5e_{d,t}\right).
\end{equation}
方案4-2中$q=g$，相应调整费用为零。规划惩罚和库存终值均不计入实际账单；所有紧急购电始终按5倍实际价格结算。场景规划与固定模式修正属于近似求解，不据此宣称全年策略全局最优。

\subsection{问题四模型的求解与结果}

\textbf{评价口径。}
一月从6000\,kWh开始采用历史基准策略预热，正式评价2025年2月1日至12月31日共334日、48096槽。方案4-2、4-3继承的期初储电量分别为1390.38、1522.74\,kWh，期末均为1200\,kWh。前者的情景混合整数规划单次限时5\,s、目标间隙0.2\%，固定模式修正最多120次迭代；后者采用HiGHS求解线性规划。

\textbf{价格预测与购电费用。}
图\ref{fig:q4-price}表明，加入供需预测特征后，各月午夜电价RMSE均下降。评价期RMSE由0.06195降至0.05614元/kWh，降幅9.38\%；MAE由0.04632降至0.04216元/kWh。保持方案4-2的供需预测与调度方法一致，使用扩展价格特征的实际费用由14018022.61元降至13994531.26元，减少23491.35元（0.168\%），其中紧急费减少17452.05元。预测误差的相对降幅明显大于总费用降幅，说明预测精度改善需经购电与储能决策才能转化为经济收益。
\begin{figure}[H]
 \centering
 \includegraphics[width=\textwidth]{q4_run/paper/figures/q4_price.pdf}
 \caption{两种午夜价格模型的预测误差。右图每组含48096槽，箱体为四分位区间，横线为中位数，须为5\%与95\%分位数；不表示置信区间。}
 \label{fig:q4-price}
\end{figure}

评价期费用与运行指标见表\ref{tab:q4-annual}，月度变化见图\ref{fig:q4-monthly}。方案4-3总费用为13579870.46元，比方案4-2低414660.79元（2.96\%），紧急购电量下降57.85\%；但方向反转由1763次增加至6625次，电芯等效满循环由465.17次增加至524.00次。两者同时存在预报权限、价格特征与控制方式差异，因此该费用差仅代表完整方案比较，不能全部归因于日内更新。循环和反转反映运行强度，不直接等同于电池寿命损失。
\input{q4_run/paper/annual_table.tex}
\begin{figure}[H]
 \centering
 \includegraphics[width=\textwidth]{q4_run/paper/figures/q4_monthly.pdf}
 \caption{评价期两方案的月度总费用与紧急费占比。费用由逐槽实际账单求和，月份间保留实际天数差异。}
 \label{fig:q4-monthly}
\end{figure}

\textbf{指定日期调度。}
表\ref{tab:q4-purchase-4-2}、\ref{tab:q4-purchase-4-3}给出四个指定日的购电结果；表\ref{tab:q4-storage-4-2}、\ref{tab:q4-storage-4-3}中各四小时时段采用“充电量/放电量”列示，末两行单列储电量。区间内两项同时非零表示不同槽的累计动作，并非同时充放电。表\ref{tab:q4-emergency-4-2}、\ref{tab:q4-emergency-4-3}列出全部连续紧急购电区间及相应电量。
\input{q4_run/paper/purchase_table.tex}
\input{q4_run/paper/storage_tables.tex}
\input{q4_run/paper/emergency_tables.tex}

以题目指定的9月23日为例，图\ref{fig:q4-dispatch}展示价格与调度的时序关系：两方案均在凌晨与午间低价阶段补充储能，在早、晚较高价格阶段放电。方案4-2受小时方向约束，方案4-3响应更频繁，形成不同的购电和库存轨迹。图中的竖线为日内发布时刻；价格预测曲线均为午夜发布，不能理解为方案4-3全天始终采用同一条价格预测。
\begin{figure}[H]
 \centering
 \includegraphics[width=\textwidth]{q4_run/paper/figures/q4_dispatch.pdf}
 \caption{9月23日的价格、购电及储能轨迹。电池功率正值为充电、负值为放电；6、12、18时竖线表示日内发布时刻，储电量水平虚线表示安全上下界。}
 \label{fig:q4-dispatch}
\end{figure}

\textbf{结果检验。}
从完整轨迹逐槽复算，两方案供需平衡与SOC递推最大残差均小于$10^{-9}$\,kWh，跨日SOC衔接误差为零，实际同时充放电及紧急电充电次数均为零，储电量和功率满足题设边界。完整购电策略分别保存于\texttt{result4-2.xlsx}和\texttt{result4-3.xlsx}。本节为单年度滚动回测，该年度数据参与过模型开发；月度一致改善不替代独立测试集检验。
