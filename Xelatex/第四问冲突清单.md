# 第四问与现有稿件的冲突清单

本清单不进入论文正文。按本次要求，第四问继续采用已确定的第八次实验成果；暂不改写其他问题或重算替代方案。基线为 `origin/main` 的 `ff5f9cd`，第四问结果取自已推送的 `459cdb2`。页数约束依据[2026年官方论文格式规范](https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html)。

| 项目 | 当前稿件或结果 | 与第四问的差异 | 后续统一时需要做的事 |
|---|---|---|---|
| 第三问的下调计费 | `sections/07-问题三.tex` 采用取消部分退回原费用50%，允许购电双向调整。 | 第八次实验4-3的历史程序将下调作为额外罚费，因此只求相对午夜原计划的上调策略。保存轨迹下调量为零，按退款规则重算仍得到13579870.46元，但这不证明退款模型的最优策略也只会上调。 | 决定第四问是否也重新求解允许下调的策略。未重算前，不将4-3称为当前第三问模型仅替换价格后的结果。 |
| 第三问的规划约束 | 当前第三问使用逐场景充放电互斥、禁止紧急电充电的混合整数规划。 | 4-3为情景线性规划近似，规划层未强制这两类互斥；其实际执行轨迹满足互斥和禁止紧急电充电。 | 若统一为第三问现行模型，需要对4-3重新调度、计费并更新图表。不得将执行可行性解释为规划场景全部物理可行。 |
| 第二问的正式方案 | `sections/06-问题二.tex` 沿用第六次实验正式主组及动态规划，总费用14066257.48元。 | 第四问4-2使用第八次实验单HGB供需预测、小时共用模式MIP和固定模式购电修正，总费用13994531.26元；此前选定的第八次实验Q2为13201981.95元，尚非本分支第二问正文。 | 后续确定第二问是否同步为第八次实验。统一前不得把Q2与4-2之差仅解释为电价改变。 |
| 第三问的正式结果版本 | `q3_run` 是main中新近完成的退款、严格MIP方案。 | 已选定exp008的旧Q3为12826263.07元，其控制器与main当前Q3不同。 | 汇总摘要、年度比较表时须明确最终版本；不要混用旧Q3数值与新Q3公式。 |
| 一月预热策略 | 主文第三问从1月起按现行模型连续运行。 | 第八次实验第四问接续历史基准预热档案；2月初SOC分别为1390.38、1522.74kWh。它们均可追溯至1月1日6000kWh，但一月未运行最终HGB调度。 | 若要求各问从1月起同一模型、相同预热方法，需要统一重跑；本次已如实记录继承初态。 |
| 总体问题分析的模型描述 | `sections/02-问题分析.tex` 声称四问同一可行域，并提及多阶段非预见性、联合价格场景压缩等。 | 第四问使用价格点预测，两方案控制器及规划可行域不同；不含联合随机价格场景或严格非预见性场景树。 | 修改问题分析时按最终实现重写相关概括，删除未执行的方法。 |
| 问题一与综合检验 | 问题一正文已出现动态规划表述，问题背景/分析及第九节仍保留其他模型说明、旧图表和“待更新”标记。 | 这些内容不能作为第四问同模型比较或最终全文一致性证明。 | 单独完成问题一和第九节统一；本次不修改其公式、数值和图表。 |
| 摘要与全篇定稿 | `sections/00-摘要.tex` 仍为占位内容；部分章节尚未完成。 | 第四问章节完成不等于全篇定稿。 | 本次整合预览为53页，附录从第45页开始；摘要后至参考文献共43页，已超过2026年正文30页上限。全文版本统一后须精简篇幅、撰写摘要，并复核图表引用与最终结果工作簿。 |

当前4-2与4-3之间总费用相差414660.79元，只能作为两套完整策略的比较，不能直接解释成额外光伏预报或日内更新的单一收益。以上冲突均留待统一处理，不以文字补丁替代必须的重新计算。

## 整篇集成检查中发现的其他待统一项

第四问独立预览已覆盖本问全部图表引用。以下为整篇原有内容的检查结果，暂列清单，不在第四问正文讨论或代改：

- 未建立正文引用的图：`fig:q1day`、`fig:q1-power`、`fig:q1-tradeoff`，以及 `fig:q3-purchase`、`fig:q3-storage`、`fig:q3-heat`、`fig:q3-monthly`。
- 未建立正文引用的表：`tab:global-symbols`、`tab:q1-stages`、`tab:q1-purchase`、`tab:q1-storage`、`tab:q1-robust`、`tab:q1-noise`；第二问四个日期各自的 `q2-purchase-*`、`q2-battery-*` 及 `tab:q2-annual`；第三问的 `q3-daily`、`q3-slots`、`q3-storage`、`q3-soc`、`q3-emergency`、`q3-prediction`、`q3-verification`、`q3-sensitivity`、`q3-comparison`；附录的 `tab:文件列表`。
- 附录文件列表所在表环境缺少题注；手工参考文献 `babic2023`、`scipymilp` 尚未在正文引用。
- 第二问未检出以 `fig:q2-` 标识的正式结果图，需核对是否未插入或标签尚未统一。
- 现有整篇内嵌位图最低约289 dpi，低于本次配图质量目标300 dpi；第四问三图均采用矢量PDF，另附450 dpi PNG。

以上是章节整合前的待办，不构成第四问费用或指定日期表需要变更的依据。全文正式定稿仍需统一这些项目，再执行整篇检查。
