# 授权范围与正文语义独立审查

结论：PASS。验收者没有编写被审正文、表格、求解程序或结果。

本轮仅精简问题二至四的文字和重复推导，保留优化目标与技术路线；合并问题三购电与紧急购电表，储能表两两并排，指定日两图可独立接续分页，并处理三问之间分页。问题一及此前章节、09–14节、求解器、输入、轨迹、工作簿与图像文件原本均不属于编辑范围。同步远程问题一后，用户明确批准“允许，仅修正这两处引用（推荐）”，因此问题一只有两处失效引用的精确替换例外。正式结果表数据必须保留，文字可以删去已经在表中给出的重复数字。本轮不进行训练或优化重跑。

主文件移除问题二、三后的两处强制分页，并只在原有问题二至四局部分组内覆盖类文件强制的表格行距，使紧凑行距设置实际生效；分组在问题四之后结束。类文件没有修改，问题一之前和问题四之后的主文件内容受精确字节比较保护。

最初基线为 `52be6e025a82b67991b0379fdbfa45623fe50cc1`。执行期间主任务两次同步远程已有的前置章节修改：中间基线为 `cfea86a8342d0feaa6d22e22d0bd78b2a5574a2e`，最终保护基线为 `b24d106715a550c16f541f1bc7b34e275d1955db`。独立比较确认三个提交的问题二至四原文和指定结果表完全相同。受保护源文件与净省页均对照最终基线；旧152页/正文46页及中间151页/正文45页不能作为最终净省页的计算起点。

## 保留目标与技术路线

- 问题二：保留历史信息限定、两树等权融合、共同岭校准及非递归前日半量残差、冷启动、完整误差路径、至多三情景共同模式MIP、固定模式L-BFGS-B修正及实际反馈。岭校准目标改为加权残差平方与正则惩罚的短文；模式变化预算及模式代理目标两个展示式逐字节保留。0.002吞吐权重、两阶段期末估值、末日置零和真实费用不计代理项的区别均保留。
- 问题三：节点校准的残差平方、12倍二阶差分平方、2倍幅度平方三项目标由短文准确表达，求解后独立乘N/(N+7)；负载18槽观察窗、6槽半衰期、36槽预测衰减，同发布时间近28日误差路径与至多7情景均保留。最终上/下调及账单展示式代数未变；LP目标全部项、权重、情景概率及期末库存价值未变，约束与20kWh实际充电死区改为紧随短文。午夜原计划、当前计划、最终结算与真实SOC的时序一致。“是否需要其他预报”的答题结论及未做逐发布时间经济消融的限制仍在正文。
- 问题四：保留8/10维因果价格回归及其优化目标、28日样本窗/14日半衰期、先验及惩罚矩阵。4-2保留小时共模、三情景、50元切换罚、0.002吞吐罚、两阶段库存估值和午夜锁定；4-3用交叉引用继承问题三LP及20kWh死区。实际账单引用公共公式并采用已实现价格，未把价格点预测或代理目标当作实际费用。所有正式结果表和四幅图仍在正文。

## 符号与删除的推导

本轮按用户授权保留6个行间公式、删除33个重复定义或推导展示式，不采用上一轮“39式逐字节不变”的验收门槛。保留式中的共同模式、路径索引、费用项、期末库存估值、发布时刻、剩余时域、情景权重、回归特征和系数等必要局部符号均在使用处说明；公共物理符号仍由未修改的第04节提供。没有要求恢复基础算子的教学解释。

## 表格与受保护文件

独立程序 `verify_source.py` 将问题三的四日期×原计划/最终购电、全天费用、四小时充放、起止SOC、紧急区间及合计解析为语义键，127个数值均与基线一致。合并表的四个原计划费用格为破折号，全天费用只填最终结算列。问题二及问题四指定表文件逐字节相同；其他正式结果表、工作簿及轨迹也受整文件保护检查。

443个受保护文件通过检查，其中440个直接与基线Git对象逐字节相同。两个原有PowerShell启动脚本由仓库属性规定采用CRLF检出，而Git对象为LF；独立检查确认只存在已声明的检出换行转换，工作树相对基线无Git差异，不属于本轮编辑。余下的第05节严格等于基线两次将 `\ref{q1-compare}` 替换为 `\ref{tab:q1-compare}` 后的字节，连末尾空行也保持一致。12个受保护章节（00–05和09–14）中11个逐字节相同，第05节仅含用户批准的两处引用修正。

活动TeX输入闭包的标签、引用、公式引用及文献引用全部闭合；全部结果图仍被正文引用。同步 b24 后发现的两处问题一失效引用，已按用户授权修复并通过严格范围核验；验收者未修改任何被审源文件。净省页及保护前缀比较采用同样修正两处引用的隔离 b24 基线。最终版面以之后的PDF扫描和实际渲染验收为准。
