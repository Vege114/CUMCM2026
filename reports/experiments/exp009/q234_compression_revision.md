# 问题二至四精简与分页调整

## 修改范围

本轮依据用户决定，压缩问题二、三、四中的数学推导、基础运算说明和重复解释，保留优化目标、技术方向、实际执行与结算规则、主要结果和必要的变量释义。暂不以正文 30 页作为本轮完成门槛。

工作期间同步了远程 `main` 的前置章节更新及 `b24d106715a550c16f541f1bc7b34e275d1955db` 的问题一更新。以该提交作为最终对照基线，不把其他人的删改计入本轮压缩量。问题一只根据用户追加确认，将两处 `\ref{q1-compare}` 修正为 `\ref{tab:q1-compare}`；其余字节不变。问题一以前各节、问题一表格，以及第九节至附录均保留最新远程版本。

## 正文调整

| 部分 | 调整 |
| --- | --- |
| 问题二 | 展示公式从 11 个减为 2 个，保留计划模式变化预算和情景规划目标；融合预测、因果校准、历史误差路径及固定模式后的购电修正改为简洁说明，删除重复设置表。 |
| 问题三 | 展示公式从 24 个减为 3 个，保留净调整、实际账单和情景 LP 目标；删除矩阵求逆、插值积分及逐项反馈推导，保留预测更新和滚动执行的关键规则。 |
| 问题四 | 展示公式从 4 个减为 1 个，保留价格估计目标；两种调度方案用文字说明目标组成和技术区别，公共结算及问题三已有目标采用引用。 |
| 指定日期结果 | 问题三的午夜原计划与最终常规购电量合为一个四日期对照表；紧急购电区间合为一个四日期表；四张储能表继续两两并排；两张指定日结果图保持完整，可分别接续分页，避免不可拆分图组造成大片留白。 |
| 分页与表格 | 删除问题二、三结束处的强制换页；在问题二至四的局部分组内取消类文件强制表格行距，默认采用 1.05，问题三合并表采用 0.96。字号、页边距及其他章节排版设置不变。 |

问题二至四共删除 33 个展示公式；保留公式在第一次出现后说明所需变量。优化费用与真实账单继续区分，信息边界、跨日实际储电量传递、日内退款规则以及数值结果均保留。未重新训练或求解任何模型，未改动计算代码、工作簿、实际轨迹或图件。

## 核验与产物

以相同两处引用修正后的最新 `main` 作为对照，完整 PDF 从 150 页降为 138 页；按摘要后至附录前的相同口径，正文从 44 页降为 32 页，净减少 12 页。附录起始页从 46 移至 34，附录内容未删改。问题一及之前的排版前缀与校正对照保持一致，本轮未通过缩小正文字号或页边距压缩篇幅。

独立审查记录位于 [q234_compression_audit](q234_compression_audit/)。源码核验逐文件比较保护范围，按日期、时段、原计划/最终量和指标核对问题三合并表中的 127 个数值，并检查问题二、四结果表及所有活动引用。问题一的唯一例外按两次精确字符串替换核验。

PDF 按 XeLaTeX 完整编译，以相同的 `SOURCE_DATE_EPOCH=1789295175` 和 `FORCE_SOURCE_DATE=1` 重现构建；构建报告、源文件绑定、实际页数、字体、位图分辨率及视觉检查均单独记录。同步生成 `Xelatex/数模通用模板.pdf`、`Xelatex/build/数模通用模板.pdf` 和根目录 `完整论文.pdf`，避免旧副本与新源码混淆。PDF、构建清单及 LaTeX 中间产物沿用仓库既有忽略规则，不纳入本次提交。

保留图件的实际最低分辨率为 289 DPI，沿用此前经核验的阈值；没有为此次精简而重采样图件。未把用户暂缓的 30 页目标列为编译失败项。
