# {{experiment_id}}：{{title}}

| 实验信息 | 内容 |
|---|---|
| 评价协议版本 | {{protocol_version}} |
| 评价日期范围 | {{evaluation_period}} |
| 实验代码提交 | {{code_commit}} |
| 数据校验值清单 | data_hashes.json |
| 环境与模型配置 | record.draft.json，登记后见 registry |

## 1. 结论与成绩

填写实测结论、评估时间范围、总费用和相对基线变化。未改善也必须说明。

{{primary_results_table}}

主表至少包含问题、计划费、调整费、紧急购电费、总费用、紧急购电量和相对基线变化。正式策略、对照与消融分别标明。

## 2. 题目指标及信息边界

逐项定义预测与调度指标、单位、分母、计费规则、可用信息、储能假设。

| 指标 | 精确定义 | 单位 | 汇总方法 |
|---|---|---|---|
| 平均绝对误差 | 绝对误差总和除以样本数 | 目标原单位 | 从误差总和重新汇总 |
| 均方根误差 | 平方误差平均后开平方 | 目标原单位 | 不直接平均月度值 |
| 加权绝对百分比误差 | 100 乘绝对误差总和除以实际值绝对值总和 | 百分比 | 零分母标为不可计算 |
| 调度总费用 | {{billing_formula}} | 元 | 逐区间结算后求和 |

## 3. 数据与时间验证

列出数据校验值、区间语义、训练与验证边界、预报发布边界和缺失值处理。

{{data_validation_table}}

## 4. 逐步技术讲解

按数据、特征、模型、预测、调度、执行、结算逐步说明：输入是什么，如何计算，为什么这样计算，输出交给谁。
解释每个公式符号、网络层、损失函数、参数更新和实际例子。算法简称不能代替解释。
配套完整技术流程图和网络结构图。

{{technical_walkthrough}}

## 5. 实验设置

记录模型配置、随机种子、设备、依赖版本、训练轮数、验证选择规则、对照及消融。

支持固定架构或候选架构两种模式。固定架构说明为何事先选定、训练组数与正式种子；候选模式才填写选模规则。随机种子用于稳定性评估、选种子或集成的角色必须明确，不能默认三类网络或三种子平均。

风险调度需记录场景历史、信息揭示顺序、非预见性约束、风险权重校准期、求解预算、可行解核验及回退机制。

{{experiment_configuration_table}}

## 6. 结果及失败案例

展示全年和分月预测误差、提前量误差、实际发电时段误差、费用分项、题目指定日期表格、紧急购电明细、储能约束及耗时。
用实测数据说明表现最差的日期和模型局限。

分别核算训练、预测、风险校准、回放、导出及报告阶段时间。展示基础预测与修正量、三层核心对照、场景树、费用与尾部风险、求解差距和回退；未知差距不填零。完整列出每个种子及均值、样本标准差。

{{forecast_results_table}}

{{seed_mean_and_standard_deviation}}

{{cost_and_constraint_results}}

{{measured_failure_case}}

[指定日期的完整表格](specified_dates.md)

## 7. 历次指标和技术路线对比

自动读取 registry 中此前全部实验；本次运行的基线和消融不冒充历史实验。
只有数据、时期、计费和协议相同的实验可以直接排名；其他实验列出差异。
首次实验写明没有历史成绩，不以零代替缺失值。

按“正式策略”角色匹配历史，允许不同网络名称。预测与调度分别检查可比性。历史预测在新协议下重算时，同时保留原登记结果与重算来源，记录插值/积分、效率、时标等转换，不能静默替换旧记录。

历史比较必须配套可视化，并在最终对话中通过 Visualize 直接展示，不能只交付文字、表格或图表链接。图中同时呈现本次和此前实验，标注实验编号、正式策略、原始值、单位及可比性；同口径时再显示绝对差和相对变化。费用优先，预测误差和各阶段耗时分别比较，改善与退步都要显示。多问题使用统一尺度的小多图，不能只展示最有利的问题或指标。

原登记结果与新协议重算结果明确分开；不可比的数据只并列说明，不绘制改善率或排名。网页、论文图和对话图使用同一份已核验数据。具体图型、数据字段与交付检查见 [历史比较可视化要求](../../templates/history-comparison.md)。

{{history_comparison}}

{{history_visualizations}}

## 8. 复现说明

列出代码提交、配置、完整运行命令、输出文件、数据校验值和已执行的验证。

{{reproduction_commands}}

{{verification_results}}
