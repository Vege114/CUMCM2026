# 新对话上下文摘要：问题二、三、四

> 用途：将本文件上传或粘贴到新对话，使其接续当前数学建模项目。  
> 本文记录已完成成果、决策依据与后续任务，不表示后续模型已经修改或重算。  
> 用户希望新对话聚焦问题二、三、四；尚未授权本次摘要生成操作直接修改这些模型。

## 一、项目位置与工作方式

项目根目录：

```text
D:\junk mass\小乱七八糟\数学建模\26国赛总赛\赛题\C题\mass
```

关键资料（以下相对路径均相对于该根目录）：

| 文件或目录 | 内容与用途 |
|---|---|
| `Xelatex/数模通用模板.tex` | 唯一主要论文源文件。用户要求论文修改直接落到这里，不接受正文分散到多个文件 |
| `Xelatex/数模通用模板.pdf` | 已编译的论文，当前主要完成问题一相关内容，不能理解为整篇论文已完成 |
| `Xelatex/code/q1_reproduce.py` | 问题一可独立运行的求解与检验代码，包含内嵌输入数据；代码也已完整收入主 TeX 附录 |
| `Xelatex/q1_run/` | 问题一方案、参数实验、噪声实验、JSON/CSV 和两幅结果图 |
| `目前整体思路最新-邱志烨/C题_完整对话与模型交接记录.md` | 较早的队友思路交接资料；与用户最新决定冲突时，以最新决定为准 |
| `reports/experiments/exp002/report.html` | 最近已阅读的队友问题二、三、四交互报告 |
| `reports/experiments/exp002/report.md` | 同包文字报告，更适合完整读取；HTML 约 30 MB，包含大量嵌入脚本和数据，不宜直接整文件输出 |
| `reports/experiments/exp002/methods.md` | 技术方法、场景控制、结算公式 |
| `reports/experiments/exp002/protocol.json` | 当前实验协议 |
| `reports/experiments/exp002/verification.json` | 队友已保存的核验结果 |
| `问题二三四统一口径与exp002修改交接报告.md` | 已制作的详细交接报告，包含公式、差异、重算步骤和验收清单，建议与本摘要一起阅读 |

原题在根目录上一级：`../C题.pdf`，附件目录为 `../附件/`。正式实现前应重新核对原题中各问的信息权限、计价时点和输出要求；不要把报告对题目的转述当作已独立核实的题面。

用户偏好：中文、详细且贴合题意、行文像正式竞赛论文而非模板堆砌；模型必须有实际意义。分析请求只分析，不擅自改代码或论文；要求写论文时直接修改指定主 TeX。保留原始资料和旧实验，不能覆盖后冒充新协议成果。

## 二、已经完成的核心成果

### 1. 问题一论文及实际求解

已直接重写主 TeX 中“问题1分析”和“问题一的模型建立与求解”，补齐：

- 分类变量与参数三线表、4 条模型假设；
- 27 个编号公式、三层求解步骤；
- 两幅计算结果图、费用与运行质量讨论；
- 12 组参数／基准方案、3 组噪声检验；
- 同一主 TeX 附录中的完整程序。

求解程序已实际运行。此前使用 Python 3.12.14、NumPy 2.5.3、SciPy 1.18.1、Matplotlib 3.11.1；工作区有 `.venv-q1` 环境。实际重跑时应重新核实环境，不假定这些版本在所有机器上都可用。

论文已经 XeLaTeX 编译并视觉检查，当时 PDF 共 33 页，问题一位于正文第 5—17 页。页码可能随后续修改变化。其他问题正文未在该次重写中扩写完成。

### 2. 队友 exp002 阅读与差异分析

已阅读其报告、方法、协议和已有核验记录，确认：预测与回放已经完成；效率已经一致；退款公式和三层结构仍与用户决定冲突。

**审查边界：没有独立重跑 exp002，也未完成实验分支源码逐行审计。** 报告列出的冻结提交为 `ab359643a10d584546f3c397d021f3ae44487a49`；其 `experiments/common/neural_v2/` 目录在当时阅读工作区未找到，应在相应实验分支寻找。不可将“报告称已验证”写成“本助手独立复现通过”。

### 3. 已交付两份交接材料

详细材料为 `问题二三四统一口径与exp002修改交接报告.md`；本文件是更适合新对话快速接手的摘要。当前未对问题二、三、四完成统一模型修改或新协议回放。

## 三、用户已经确定的三条要求：必须执行

### 要求 A：往返效率为 90%

\[
\eta_c=\eta_d=\sqrt{0.9},\qquad\eta_c\eta_d=0.9.
\]

交流侧充放电功率下：

\[
E_{t+1}=E_t+\eta_cP_t^{\rm ch}\Delta t
-\frac{P_t^{\rm dis}\Delta t}{\eta_d},\qquad\Delta t=1/6\ \mathrm h.
\]

若充放电变量已是区间电量，不再乘时间步长。禁止两侧分别采用 0.9，否则往返效率变为 0.81。

### 要求 B：问题一的三层优化贯穿后续问题

用户原意：问题一内容必须继承；费用公式可随问题变化，但第二、第三层必须贯彻。

统一主线为：

**第一层费用最小 → 第二层在费用预算内改善电池运行 → 第三层保留前两层成果后减少购电量。**

不能改成一次加权求和；不能把第二、第三层删掉；也不能把“三组对照实验”称为已继承“三层规划”。由于含二元状态变量，规范名称为三层混合整数线性规划。

### 要求 C：下调只退还下调部分原金额的 50%

暂沿用报告的上调及紧急购电单价，仅修正已确定的下调项：

\[
C=\sum_t\left[p_tg_t^0+1.5p_t(g_t-g_t^0)^+
-0.5p_t(g_t^0-g_t)^++5p_te_t\right].
\]

\(g_t^0\)：原申报量；\(g_t\)：最终执行前计划；\(e_t\)：紧急购电。

例：原购电 100 kWh，下调至 80 kWh，单价 1 元，无紧急购电，费用为 90 元，不是 110 元。

注意：以最终量为基数的 \(pg+0.5p(g^0-g)\) 与退款式等价，不要只凭正号判断重复收费；exp002 的问题在于它以原计划全额为基数仍加上下调项。

价格时点、上调及紧急规则、中间版本是否仅按最终净调整结算，仍需按原题核实并写清。上述简式不替代这一步。

## 四、问题一必须继承的具体数学结构

### 1. 参数与约束

每日 144 段，功率上限 5000 kW，储电量范围 1200—10800 kWh。现有主方案：

| 参数 | 当前值 | 含义 |
|---|---:|---|
| \(\delta\) | 0.001 | 第一层费用相对让步预算 |
| \(\epsilon_C\) | \(10^{-4}\) 元 | 费用数值容差，不是经济让步 |
| \(\epsilon_S\) | \(10^{-7}\) | 第二层目标保留容差 |
| 功率变化上限 | 1000 kW／10 min | 相邻区间净电池功率指令差 |
| 最短同模式运行 \(U\) | 3 段 | 30 分钟 |
| 最短同模式关闭 \(D\) | 2 段 | 20 分钟 |
| 短段参照 \(K\) | 3 段 | 短运行缺额统计 |

保留功率／能量平衡、效率、充放电互斥及运行约束。运行参数是已选运行偏好，不是假装题面给定的硬件定律；后续不能默默删除或改值。

### 2. 三层公式

同一可行域 \(\mathcal X\) 下：

\[
C^\star=\min_{x\in\mathcal X}F_1(x).
\]

问题一 \(F_1=\sum_t p_tg_t\)，后续替换为对应场景的正确费用。

\[
\mathcal V=\sum_t|P_t^{\rm bat}-P_{t-1}^{\rm bat}|,
\quad N_s=\text{启动次数},
\quad B=\sum_j(K-\ell_j^{\rm run})^+.
\]

\[
F_2=\frac{\mathcal V}{2P_{\max}T}+\frac{N_s}{2T}
+\frac{B}{2T(K-1)}.
\]

\[
S^\star=\min F_2,
\quad F_1\le(1+\delta)C^\star+\epsilon_C.
\]

\[
\min F_3=\sum_tg_t,
\quad F_1\le(1+\delta)C^\star+\epsilon_C,
\quad F_2\le S^\star+\epsilon_S.
\]

主方案 \(U=K=3\)，短段缺额为零，不能虚构该项在主方案仍产生额外下降。运行质量指标不等于经过校准的电池寿命或退化成本。

### 3. 边界不是机械照搬每日循环

问题一典型日要求初末 6000 kWh 一致，并采用循环边界；现有正文已说明后续随机条件按实际储电量跨日传递。因此“全部继承”不应解释为每天凭空重置电量。

后续必须传递真实储电量、上一时段功率、运行模式、已运行／关闭时长和未完成的最短时长承诺。不要在每次滚动窗口末端通过截断规避约束。终端目标如需调整，应说明依据并由用户确认必要的设计变更。

### 4. 已核实的问题一数值与口径

- 改进方案费用：34115.03245097 元。
- 改进方案购电：57560.61607674 kWh。
- 原始经济基准费用：33801.49564222 元。
- 新运行约束下第一层最优费用：34080.95139957 元。
- 总变差由约 102684.58 降至 33209.31，下降约 67.66%；启动次数由 12 降至 4。
- 相对原始经济基准总费用增加约 **0.928%**，不是 0.1%。0.1% 只对应同一新可行域下的层间费用让步。

这些只是问题一结果，不能移植成后续结果。

## 五、exp002 的真实进度与技术路线

### 1. 数据与预测

正式评价为 2025 年 2—12 月，共 334 天，1 月为预热、训练及校准。不要不加说明地称为 365 天全年评价。

固定四分支轻量 MLP：每分支 16→32→16→1，总参数 4356。用周期基础预测加网络残差修正，按月训练，保留过去七天验证集并冻结当月模型。正式种子 42；另外两个种子 2026、3407 用于稳定性检验，不是正式集成。

33 组训练已经完成，累计约 11.26 分钟；旧版 165 组约 98.36 分钟。训练时间下降不等于全流程同幅下降。

正式 WAPE：负载 3.5449%，电价 5.4940%，历史光伏 7.2505%，光伏预报修正 6.6501%。

### 2. 场景与执行

使用过去最多 56 个完整日期的联合残差，最多抽取 16 条路径。问题三、四—三在 6、12、18 时建立信息分支；共享当前信息的场景必须共享相应购电决策。

固定电价场景不能用未来可变价格信息分支；场景残差窗口必须完整实现；裁剪前不可观测的负值差异不能被当成新信息。上述因果检查值得保留。

当前储能执行却采用“净盈余就尽量充电、净不足就尽量放电、不足再紧急购电”的确定性规则。这不是问题一的三层运行优化，可能重新产生频繁切换。

### 3. 求解与证书

采用场景期望费用加 \(\lambda\operatorname{CVaR}_{0.9}\)，两秒求解器预算，先获取区域 LP 可行候选，再尝试完整 MILP。

正式种子四个场景合计 3340 次风险求解：3339 次超时，0 次取得不超过 1% 的全局差距证书，0 次真正回退。可行候选可用于执行，因此超时和未回退并不矛盾；但不能称为全局最优。

风险权重：问题二为 0.1，问题三、四—二、四—三均为 0。后三种场景不能宣传为非零 CVaR 惩罚产生了收益。

## 六、对 exp002 的关键判断与依据

### 判断 1：预测部分可保留，调度必须重构统一

依据：效率已经一致；时间验证、光伏积分、信息隔离和轻量预测可独立于调度使用。但报告没有问题一的第二、第三层，结算公式还采用额外下调收费。

报告的“三层核心对照”只是旧预测＋基础调度、新预测＋基础调度、新预测＋风险调度三组实验，绝不是三层顺序优化。

### 判断 2：不能仅更换退款符号后重算总费用

依据：费用改变会改变上调／下调的决策激励，旧计划未必仍然合适。应修改优化目标、执行核算和导出，再重做调度与风险权重校准。旧轨迹用新规则核算只能叫诊断结果。

### 判断 3：复杂调度尚未证明全面优于基础方法

下表均为 exp002 旧协议成绩，不是统一后成绩：

| 场景 | 新预测＋基础调度／元 | 正式场景调度／元 | 正式策略相对变化 |
|---|---:|---:|---:|
| 二 | 15352302.2799 | 15128897.4527 | -1.455% |
| 三 | 13257848.3582 | 14005062.7759 | +5.636% |
| 四—二 | 16114809.6566 | 15901397.2011 | -1.324% |
| 四—三 | 14026450.8280 | 14734925.1249 | +5.051% |

问题三、四—三不仅总费用变差，尾部日费用也变差。紧急购电费分别增加约 89.86 万元、88.13 万元，是可核算的主要恶化账目，但不能据此唯一判定是场景树、近似方法或求解时限导致。

### 判断 4：因果性不要求必须使用贪心电池控制

依据：只使用当前信息、冻结历史动作并实施非预见性约束的滚动优化，同样可以因果。需要让实际执行遵守运行要求，而不是仅让计划曲线平滑、执行时再换成无约束贪心。

问题二、四—二的购电计划不能因为电池实时反馈而擅自变成日内可修改；应区分购电更新权限与电池控制权限。

### 判断 5：不能把未证明最优的第一层可行值叫作严格费用基准

依据：三层预算使用的 \(C^\star\) 有最优值语义；若仅有可行 incumbent，应报告上下界、差距和相对该可行值的预算，不能宣称严格近最优保证。需要合理增加预算或调整求解规模并验证。

## 七、新对话优先开展的工作

1. **先读原题、主 TeX 问题一、问题一代码及详细交接报告。** 确认当前文件是否已有用户或队友更新，不按本摘要覆盖较新成果。
2. 找到 exp002 对应实验分支，定位费用、控制器、MILP 和导出代码；说明已读与未读范围。
3. 给出统一三层模型的实际接入方案，明确计划与执行、跨日状态、购电更新权限。
4. 先做退款手算、短时段可穷举、跨日和突发偏差测试，再做评价期回放。
5. 复用已冻结预测，先完成“新预测＋统一三层调度”基线，不必因结算变化盲目重训网络。
6. 然后分别测试场景树、风险项、场景数和求解预算的贡献，保留不利结果。
7. 修改目标后重新校准风险参数，在新协议下重做对照和导出，保留 exp002 原档案。

### 需要明确而不能擅自决定的事项

- 随机场景下第二层运行质量如何聚合，第三层是否含紧急购电及如何统计。
- 第一步若使用期望费用加 CVaR，0.1% 预算约束的是复合目标还是纯费用，不能混称。
- 滚动窗口的费用基准，不得每次机械在前次已放宽费用上再加 0.1%；局部预算不等于累计评价期费用保证。
- 极端误差下如运行约束与供需冲突，如何借助紧急购电、弃电保证可行；如确需应急松弛，必须说明条件并交用户确认，不默默删约束。
- 终端储能处理与跨窗口运行承诺。

## 八、交付与验收底线

新结果至少同时报告：费用分项及总费用、购电及紧急购电、弃电、功率总变差、启动次数、短段缺额、最大功率变化、最短时长违规、能量残差、期末状态，以及各层求解状态、耗时、差距证书。

独立核验必须针对**实际执行轨迹**，并覆盖题目约束及继承的运行要求；不能只以旧核验的“零违规”证明新约束已满足。

比较实验必须统一评价日期、信息权限、效率、结算、初始状态、执行规则及终端处理。未知差距不能填零，限时可行解不能冒充最优解，预测改善不能自动等同于费用改善。

如果用户随后要求论文写作，直接编辑 `Xelatex/数模通用模板.tex`，并保持问题一与后续各问的符号、效率、费用和三层主线一致。当前摘要生成不授权立即进行代码、论文或大规模实验修改。

## 九、可直接粘贴给新对话的开场说明

> 请先阅读我上传的《新对话上下文摘要_问题二三四.md》和《问题二三四统一口径与exp002修改交接报告.md》，接手本项目的问题二、三、四。我的三条确定要求是：往返效率 90%；问题一三层混合整数线性规划贯穿后续各问；下调购电仅退还下调部分原金额的一半。请先核对当前原题、论文、问题一代码和队友最新实验，区分已实现成果与尚需修改内容，反馈接入方案及真正需要我决定的事项。不要仅因为文件写有“三层对照”就认为继承了三层优化，也不要将旧协议成绩当作新结果。未经我的后续实施要求，先不要覆盖论文、代码或旧实验。
