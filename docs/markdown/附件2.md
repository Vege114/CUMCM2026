**模拟器通信接口说明及编程指南**

<a id="X401d761c4b6881dbd788b13f1ff2097b7c3ef61"></a>
# **1. 基本设定**

<a id="X71ae463eea0cf46aba635c6c7b189aefd3debe9"></a>
# **1.1. 目标区域与坐标系**

测试在一个二维虚拟目标区域中进行。

- 目标区域是半径 1800 米的圆形区域。
- 所有干扰源位于目标区域内。
- 目标区域圆心是坐标原点 $(0,0)$ 。
- $x$  轴正方向为东。
- $y$  轴正方向为北。
- 坐标单位为米。
- 机器狗可以向模拟器提交目标区域外的位置；模拟器仍会按提交的位置计算移动耗时和检测、清除结果。
- 机器狗位置不构成正常策略约束，但每个坐标分量必须是有限数值，且绝对值不得超过2000000米。NaN、正负无穷大和超出范围的坐标返回HTTP 400，请求不执行，也不推进位置或虚拟时间。

<a id="Xda136eb2af12b80120d4d9145c971017ffddfc3"></a>
# **1.2. 角度约定**

所有方位角都采用以下约定：

- 正东方向为 $0^{\circ }$ 。
- 逆时针方向为正。
- 正北方向为 $90^{\circ }$ 。
- 正西方向为 $180^{\circ }$ 。
- 正南方向为 $270^{\circ }$ 。
- 角度范围为 $[0,360)$ 。

/measure 返回的 svd\_deg 表示干扰源的示向度，单位为度。

<a id="X2f02cdb40c0317faba08000a78f02b904ec2c0d"></a>
# **1.3. 频道**

- 频道用整数表示。
- 有效频道范围是 $\{1,2,\cdots ,20\}$ 。
- 每个频道最多对应一个干扰源。
- 一次测试中干扰源总数量在 10 到 16 个之间。
- 具体有多少个干扰源不会通过接口直接返回，机器狗程序需要根据检测和清除结果自行判断。
- /measure 的 channel 表示测向机要检测的频道。
- /clear 的 channel 表示要尝试清除的目标干扰源频道。

注意：/clear 的 channel 不会改变测向机当前频道，也不会产生切换频道耗时。

<a id="X5558fe700ee377b0115765ded336698d44dd21a"></a>
# **1.4. 初始状态**

机器狗成功调用 /enter 后，模拟器初始状态如下，并开始计时：

- 初始位置：(0,0)。
- 测向机初始频道：1。
- /enter 本身不推进虚拟时钟。
- 每次测试只允许一个与当前登录参赛队号一致的机器狗进入。机器狗必须逐次发送动作，并在收到本次完整HTTP响应后再发送下一个新动作。正常串行合法请求没有请求速率上限；每次检测的5秒只增加虚拟时间，不要求在现实中等待5秒。

<a id="Xc33161192edbc764739a8f49d7fd1532ad2f5b2"></a>
# **1.5. 其他信息**

- 模拟器默认服务地址：http://127.0.0.1:2026。
- 选手可以在模拟器空闲时修改端口。
- 模拟器只监听本机回环地址，其他电脑不能直接访问该接口。
- 非测试期间、5秒倒计时期间以及测试结束后，机器狗接口未开放，连接可能直接失败。

# **2. 关键物理规则**

<a id="X0b668d4caa341e87d795371b606920ad3059f74"></a>
# **2.1. 最大可被有效接收距离**

每个干扰源都有一个最大可被有效接收距离，简称有效接收半径。

- 该距离在 1000 米到 1500 米之间。
- 距离超过该干扰源的有效接收半径时，机器狗在该位置接收不到该频道信号。
- 该距离不是固定常数，不同干扰源可能不同。
- 模拟器接口不会返回某个干扰源的有效接收半径。

<a id="X1483c58b8720cc9d677e194b6adf5c4a1f82637"></a>
# **2.2. 全向与定向干扰源**

干扰源分为全向和定向两类。

- 全向干扰源：检测点到干扰源的距离不超过有效接收半径时，可以检测到信号。
- 定向干扰源：检测点必须同时满足距离条件，并且位于该干扰源的有效覆盖角度范围内，才能检测到信号。有效覆盖角度范围为 $180^{\circ }$ （包含边界），定向方向未知。

因此，/measure 返回无信号时，只能说明当前位置接收不到该频道信号。可能原因包括：

- 该频道没有未清除干扰源。
- 距离超过有效接收半径。
- 该频道干扰源是定向干扰源，但当前位置不在它的覆盖范围内。

<a id="Xba91fd1059596e01101e3110404cc5d73f90667"></a>
# **2.3. 示向度误差**

/measure 返回的 svd\_deg 是示向度，不是干扰源的精确真实方位。

- svd\_deg 表示在检测点处测得的干扰源的示向度，单位为度。
- 模拟器返回的 svd\_deg 与干扰源的最大场强方向的真实方位角之间存在一定的误差，参见题目正文。
- 误差范围为 $[-1^{\circ }, 1^{\circ }]$ 。
- 返回值保留两位小数。
- 返回值仍按 $[0,360)$  归一化。

机器狗程序不应把单次 svd\_deg 当作完全精确的真实方位。

<a id="Xbd2c8cbbc2158d3b00c284143313c55a0d89f68"></a>
# **2.4. 近距离阈值与清除半径**

- 近距离阈值为 5 米。检测距离不超过 5 米并且位于干扰源信号覆盖角度范围内时，由于信号太强，将无法获得示向度信息，/measure 返回 measure\_result="near"，不返回 svd\_deg。
- 清除半径为 20 米。清除位置与目标干扰源距离小于等于 20 米时，/clear 可以清除成功。注：光学精确定位及清除操作是否成功仅与清除点与干扰源之间的距离有关，与定向干扰源的朝向无关。

# **3. 指令**

机器狗程序只需要调用 4 条 HTTP 接口：

<table><thead><tr><th><p>指令</p></th><th><p>含义</p></th><th><p>是否推进虚拟时钟</p></th></tr></thead><tbody><tr><td><p>POST /enter</p></td><td><p>进入目标区域，开始本次运行。</p></td><td><p>否</p></td></tr><tr><td><p>POST /measure</p></td><td><p>到指定位置，对指定频道检测。</p></td><td><p>是</p></td></tr><tr><td><p>POST /clear</p></td><td><p>到指定位置，尝试清除指定频道的干扰源。</p></td><td><p>是</p></td></tr><tr><td><p>POST /exit</p></td><td><p>退出目标区域，结束本次运行。</p></td><td><p>否</p></td></tr></tbody></table>

移动和切换频道没有单独的接口：

- 移动由 /measure 或 /clear 请求中的 position 体现。
- 切换测向机频道只由 /measure 请求中的 channel 体现。

# **4. 虚拟时间计算方法**

<a id="X10cda5e2286c40128c93a38995766f4ea7dac10"></a>
# **4.1.虚拟时钟推进规则**

只有请求被模拟器接受，即响应中 accepted=true 时，才可能推进虚拟时钟。

- /enter：accepted=true 时也不推进虚拟时钟。
- /exit：accepted=true 时也不推进虚拟时钟。
- /measure：accepted=true 时推进虚拟时钟。
- /clear：accepted=true 时推进虚拟时钟。
- accepted=false：本次请求不生效，虚拟时钟不推进。

accepted=false 的响应里 virtual\_time\_s 为 0。不要把这个 0 当作当前虚拟时刻。需要记录虚拟时刻时，应使用最近一次 accepted=true 的响应。

virtual\_time\_s 是JSON number，可能带小数，不应按整数解析。模拟器内部按微秒累计，响应最多保留6位小数，并删除无意义的末尾零。

<a id="X6301dd754f62789f9a223bff22dfc1f477f7157"></a>
# **4.2. 移动耗时**

模拟器会记录机器狗上一次合法动作的位置。

一次合法 /measure 或 /clear 的移动耗时为

$$
移动耗时=\frac{两次位置之间的直线距离}{移动速度}
$$

默认移动速度：5 m/s

例如，从 $(0,0)$  移动到 $(300,400)$ ，直线距离为 500 米，移动耗时为

$$
\frac{500}{5}=100 (秒)
$$

<a id="X2afd564c6041ae9534500acd4cf578b2146a627"></a>
# **4.3. 检测耗时**

一次合法 /measure 的总耗时为

$$
检测总耗时=移动耗时+切换频道耗时+检测动作耗时
$$

默认值：

- 检测动作耗时：5 秒。
- 切换频道耗时：1 秒。

切换频道耗时只看合法 /measure：

- 如果本次合法 /measure 的 channel 与测向机当前频道不同，切换频道耗时为 1 秒。
- 如果频道相同，切换频道耗时为 0 秒。
- 合法 /measure 完成后，测向机当前频道会更新为本次检测频道。

<a id="X4a977688a42d8f3696d5f5b0cc1b7459b3dd1ab"></a>
# **4.4. 清除耗时**

一次合法 /clear 的总耗时为

$$
清除总耗时=移动耗时+清除动作耗时
$$

默认值：

- 未发现可清除目标时，只有精确定位动作，没有清除动作，耗时为 3 秒。
- 成功清除时，精确定位与清除两个动作总耗时为 5 秒。

/clear 不产生切换频道耗时，也不会改变测向机当前频道。

<a id="X24353ddb8c9d2a6e5280066506e5294e163efdd"></a>
# **4.5. 现实时间和虚拟时间**

用户在模拟器中确认开始后，模拟器先准备数据。数据准备完成后显示5秒倒计时。倒计时结束时开启25分钟测试窗口并开放机器狗接口。

机器狗成功调用 /enter 后开始计算最长20分钟的程序运行时间。实际现实截止时刻是“25分钟窗口截止”和“/enter 成功后20分钟”两者中较早的时刻。/enter 成功响应中的 remaining\_real\_duration\_s 是本局实际还能使用的完整现实时间秒数，范围为0至1200；机器狗程序应使用该值，不应固定假定每次都有1200秒。

虚拟世界限时为360000秒。达到现实时间或虚拟时间截止条件后，模拟器结束测试并关闭机器狗接口。截止前已经完整到达并被登记的唯一动作允许完成；在截止时刻或之后才完整到达的请求不再执行，连接可能直接关闭。测试结束后不能再调用 /exit 查询原因，具体结束原因由模拟器界面显示。

# **5. 通信格式总规则**

<a id="X2137d168443dd0a69e4c4b8042d915165b620e3"></a>
# **5.1. 请求格式**

4条指令都使用 HTTP+JSON。

- 方法：POST
- 请求头：Content-Type 必须是 application/json，可以带且只允许参数 charset=utf-8。出现其他参数时返回 HTTP 415。
- Content-Encoding 可以省略，或为 identity。其他值返回 HTTP 415。
- 请求体必须是无 BOM 的 UTF-8 JSON 对象，不能有重复键；JSON 嵌套不超过16层；请求体最大65536字节。
- 路径必须精确为 /enter、/measure、/clear 或 /exit，不接受尾随斜线和查询参数。
- 请求体必须是UTF-8 JSON对象，不能有重复键；请求体最大65536字节。
- channel 必须是 1 至 20 的整数。写成 JSON number 1.0 且数值恰好为整数时也会被接受；如写成1.5 则返回 HTTP 400。
- 未声明的JSON字段不会被忽略，而是返回HTTP 200和 accepted=false，用于帮助发现字段拼写错误。
- arena\_id 必须是ASCII字符串“default”。
- robot\_id 必须逐字节等于当前登录参赛队号，UTF-8长度为1至64字节。
- request\_id 是当前测试会话内的幂等键，UTF-8长度为1至128字节。robot\_id 和 request\_id 不能包含控制字符或不可见格式字符。

<a id="Xdeeff3aba0c2c816a48fb227272cd6accfd6cd3"></a>
# **5.2. 响应格式**

业务响应也是 JSON 对象。

所有 JSON 业务响应都有以下字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>含义</p></th></tr></thead><tbody><tr><td><p>accepted</p></td><td><p>boolean</p></td><td><p>模拟器是否接受本次请求。</p></td></tr><tr><td><p>real_timestamp_ms</p></td><td><p>number</p></td><td><p>模拟器响应时的现实世界时间戳，单位为毫秒。</p></td></tr><tr><td><p>virtual_time_s</p></td><td><p>number</p></td><td><p>虚拟时钟，单位为秒。</p></td></tr></tbody></table>

如果 accepted=false，响应只包含上面 3 个字段，不包含检测结果、清除结果或退出原因。

<a id="Xd9cb57ec25bcb46fb4b45873c18ad06d3f3def0"></a>
# **5.3. HTTP状态和业务接受状态**

<table><thead><tr><th><p>HTTP状态</p></th><th><p>含义</p></th></tr></thead><tbody><tr><td><p>200，accepted=true</p></td><td><p>动作已执行。</p></td></tr><tr><td><p>200，accepted=false</p></td><td><p>JSON结构合法，但当前测试状态拒绝动作；或存在未知字段；或 arena_id、robot_id 不匹配。</p></td></tr><tr><td><p>400</p></td><td><p>JSON语法、重复键、缺失字段、字段类型、标识符格式、频道或坐标范围错误。</p></td></tr><tr><td><p>404</p></td><td><p>路径未知或路径不精确。</p></td></tr><tr><td><p>405</p></td><td><p>已知路径使用了非POST方法。</p></td></tr><tr><td><p>409</p></td><td><p>同一 request_id 对应了不同动作，或并发发送了不同动作。</p></td></tr><tr><td><p>413</p></td><td><p>请求体超过65536字节。</p></td></tr><tr><td><p>415</p></td><td><p>Content-Type 或 Content-Encoding 不受支持。</p></td></tr><tr><td><p>429</p></td><td><p>连接/无效流量保护触发，或本局幂等记录达到上限。</p></td></tr><tr><td><p>500</p></td><td><p>模拟器内部错误。</p></td></tr></tbody></table>

所有能够形成HTTP响应的错误都返回JSON，其中至少包含 accepted、real\_timestamp\_ms 和 virtual\_time\_s。机器狗程序必须同时检查HTTP状态和 accepted，不能只判断其中一个。

每个新动作必须使用新的 request\_id。只有在网络超时、连接中断等情况下重试完全相同的动作时，才复用原请求内容和原 request\_id。同一ID、同一内容返回第一次的完整响应，不重复移动、检测、清除、退出或推进时间；同一ID改动路径、位置、频道等内容返回HTTP 409。结构错误、未知字段以及 arena\_id、robot\_id 不匹配不占用该ID，修正请求后可以复用。正常串行合法动作不设速率上限，但不同新动作不得并发发送。

并非所有错误都能形成HTTP响应。倒计时未结束、接口尚未开放、测试已经结束，或模拟器内部无法继续服务时，连接可能被直接关闭，此时没有JSON体。机器狗程序必须同时处理“连不上”和“收到JSON”。

结构错误（HTTP 400）以及未知字段、arena\_id / robot\_id 不匹配（HTTP 200且 accepted=false）不占用该 request\_id，修正后可以复用。业务上已被接受的动作占用该 request\_id。

<a id="X9ddea07f1c26c3e54cd66c604edf001f4c64b17"></a>
# **6. POST /enter：进入目标区域**

<a id="Xc27b4752bc9a20da5b310bc62c101db53e5ac69"></a>
# **6.1. 请求**

发送字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>必填</p></th><th><p>取值说明</p></th></tr></thead><tbody><tr><td><p>arena_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>必须是 "default"。</p></td></tr><tr><td><p>robot_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>机器狗标识，非空字符串，参赛队号。</p></td></tr><tr><td><p>request_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>请求标识，非空字符串。</p></td></tr></tbody></table>

请求示例：

{
  "arena\_id": "default",
  "robot\_id": "<参赛队号>",
  "request\_id": "enter-1"
}

<a id="X13574779dd19513c39b9b5154c95190956d402f"></a>
# **6.2. 响应**

accepted=true 时，返回字段：

响应示例：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 0,
  "max\_virtual\_duration\_s": 360000,
  "max\_real\_duration\_s": 1200,
  "remaining\_real\_duration\_s": 1200
}

<a id="Xd843dd420b1c7d446d72f39e672760079f2d49b"></a>
# **6.3. 请求未执行的常见原因**

# HTTP 200且 accepted=false：

## arena\_id 不是 "default"。

## robot\_id 与当前登录参赛队号不一致。

## 请求包含未声明字段。

## 重复调用 /enter。

# HTTP 400：

## 请求体不是合法 JSON，或含有重复键。

## 缺少 arena\_id、robot\_id 或 request\_id。

## robot\_id 或 request\_id 为空、超长，或含控制/格式字符。

# 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应。

# **7. POST /measure：检测**

<a id="X4ebe72f7d7f33b1050dcbfab06aa424eae8bd21"></a>
# **7.1. 请求**

发送字段：

请求示例：

{
  "arena\_id": "default",
  "robot\_id": "<参赛队号>",
  "request\_id": "measure-1",
  "position": {"x": 300, "y": 400},
  "channel": 1
}

<a id="X91a7aa7ddae356fd4d01e2198d899c62d9e3f4e"></a>
# **7.2. 响应字段**

accepted=true 时，返回字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>取值说明</p></th></tr></thead><tbody><tr><td><p>accepted</p></td><td><p>boolean</p></td><td><p>true。</p></td></tr><tr><td><p>real_timestamp_ms</p></td><td><p>number</p></td><td><p>现实世界时间戳，单位为毫秒。</p></td></tr><tr><td><p>virtual_time_s</p></td><td><p>number</p></td><td><p>本次检测完成后的虚拟时刻。</p></td></tr><tr><td><p>measure_result</p></td><td><p>string</p></td><td><p>检测结果，取值为 "no_signal"、"near" 或 "direction"。</p></td></tr><tr><td><p>svd_deg</p></td><td><p>number</p></td><td><p>示向度，单位为度，保留两位小数。只有 measure_result="direction" 时返回。</p></td></tr></tbody></table>

<a id="Xb0042031146ca23f315235bceea260adec19543"></a>
# **7.3. 三种检测结果**

无信号：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 105,
  "measure\_result": "no\_signal"
}

含义：当前位置接收不到该频道的干扰源信号。可能原因包括该频道没有未清除干扰源、距离超过最大可被有效接收距离、定向干扰源没有覆盖该位置等。

距离过近：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 105,
  "measure\_result": "near"
}

含义：已检测到该频道信号，但距离干扰源不超过 5 米。此时不返回 svd\_deg。

正常返回示向度：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 105,
  "measure\_result": "direction",
  "svd\_deg": 123.45
}

含义：已检测到该频道信号，并返回从检测点指向干扰源的方位角。svd\_deg 保留两位小数，包含误差，参见题目正文。

<a id="X1e1405e655e45663e9e28bf0be7c9bfd7030491"></a>
# **7.4. 请求未执行的常见原因**

# HTTP 200且 accepted=false：

## 尚未成功调用 /enter。

## arena\_id 不是 "default"。

## robot\_id 与当前登录参赛队号不一致。

## 请求包含未声明字段，包括 position 下的未知字段。

## 机器狗已经退出，或测试已经因超时等原因结束。

# HTTP 400：

## 请求体不是合法 JSON，或含有重复键。

## 缺少 position、position.x、position.y 或 channel。

## position.x 或 position.y 不是有限数值（含 NaN、正负无穷大），或绝对值超过2000000。

## channel 不在 1..20 范围内，或不是整数。

## robot\_id 或 request\_id 格式不合法。

# 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应

# **8. POST /clear：清除**

<a id="Xc976b1c85dc6c855bc30b3fa7beebc8bc0fb47a"></a>
# **8.1. 请求**

发送字段：

请求示例：

{
  "arena\_id": "default",
  "robot\_id": "<参赛队号>",
  "request\_id": "clear-1",
  "position": {"x": 300, "y": 0},
  "channel": 3
}

<a id="Xa61d27bb086d2c24f968e59d62b9be561e7feee"></a>
# **8.2. 清除规则**

- 清除半径是 20 米。
- 如果指定频道的未清除干扰源距离 position 小于等于 20 米，则清除成功。注意，无论全向还是定向干扰源，只要指定频道的干扰源距离小于等于 20 米，都能够精确定位并清除，不受定向干扰源有效覆盖角度范围的限制。
- 如果该频道不存在干扰源、干扰源已经被清除、或干扰源距离超过 20 米，都视为未发现可清除目标。
- 同一个干扰源只能清除一次。重复清除已被清除的干扰源，会返回未发现可清除目标。
- /clear 不会切换测向机当前频道。

<a id="X1c66d42f709b4438b22e6a1520c3b9b8b649336"></a>
# **8.3. 响应字段**

accepted=true 时，返回字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>取值说明</p></th></tr></thead><tbody><tr><td><p>accepted</p></td><td><p>boolean</p></td><td><p>true。</p></td></tr><tr><td><p>real_timestamp_ms</p></td><td><p>number</p></td><td><p>现实世界时间戳，单位为毫秒。</p></td></tr><tr><td><p>virtual_time_s</p></td><td><p>number</p></td><td><p>本次清除完成后的虚拟时刻。</p></td></tr><tr><td><p>clear_result</p></td><td><p>string</p></td><td><p>清除结果码。只有 "success" 和 "no_target_in_range"。</p></td></tr></tbody></table>

# **8.4. 两种清除结果**

成功清除：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 196,
  "clear\_result": "success"
}

对应含义：

- clear\_result="success"。
- 清除动作耗时为 5 秒，再加移动耗时。

未发现可清除目标：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 194,
  "clear\_result": "no\_target\_in\_range"
}

对应含义：

- clear\_result="no\_target\_in\_range"。
- 清除动作耗时为 3 秒，再加移动耗时。

<a id="Xd4f265a0e7ddc5020e479470f25503ef1020a6e"></a>
# **8.5. 请求未执行的常见原因**

与 /measure 相同，见第7.4节。/clear 的 channel 表示要尝试清除的目标干扰源频道，同样必须是 1..20 的整数；该字段不会切换测向机当前频道。

# **9. POST /exit：退出目标区域**

<a id="X8112616a37a5c9bc2b7155219cb16d2535a2620"></a>
# **9.1. 请求**

发送字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>必填</p></th><th><p>取值说明</p></th></tr></thead><tbody><tr><td><p>arena_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>必须是 "default"。</p></td></tr><tr><td><p>robot_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>机器狗标识，非空字符串，参赛队号。</p></td></tr><tr><td><p>request_id</p></td><td><p>string</p></td><td><p>是</p></td><td><p>请求标识，非空字符串。</p></td></tr></tbody></table>

请求示例：

{
  "arena\_id": "default",
  "robot\_id": "<参赛队号>",
  "request\_id": "exit-1"
}

<a id="Xce8b53b24daefd095b7b1fa0bc284fb2f0589e3"></a>
# **9.2. 响应**

accepted=true 时，返回字段：

<table><thead><tr><th><p>字段</p></th><th><p>类型</p></th><th><p>取值说明</p></th></tr></thead><tbody><tr><td><p>accepted</p></td><td><p>boolean</p></td><td><p>true。</p></td></tr><tr><td><p>real_timestamp_ms</p></td><td><p>number</p></td><td><p>现实世界时间戳，单位为毫秒。</p></td></tr><tr><td><p>virtual_time_s</p></td><td><p>number</p></td><td><p>退出时的虚拟时刻；/exit 不推进虚拟时钟。</p></td></tr><tr><td><p>exit_reason</p></td><td><p>string</p></td><td><p>退出原因。</p></td></tr></tbody></table>

exit\_reason 可能取值：

<table><thead><tr><th><p>取值</p></th><th><p>含义</p></th></tr></thead><tbody><tr><td><p>user_exit</p></td><td><p>主动调用 /exit 退出。</p></td></tr></tbody></table>

/exit 与其他动作使用相同的 request\_id 幂等规则。超时和手工中止会直接结束测试并关闭接口，不通过事后 /exit 返回原因。

响应示例：

{
  "accepted": **true**,
  "real\_timestamp\_ms": 1760000000000,
  "virtual\_time\_s": 199,
  "exit\_reason": "user\_exit"
}

<a id="Xa433fea4970245f314f5dc407861e3073421d79"></a>
# **9.3. 请求未执行的常见原因**

# HTTP 200且 accepted=false：

## 尚未成功调用 /enter。

## arena\_id 不是 "default"。

## robot\_id 与当前登录参赛队号不一致。

## 请求包含未声明字段。

# HTTP 400：

## 请求体不是合法 JSON，或含有重复键。

## 缺少 arena\_id、robot\_id 或 request\_id。

## robot\_id 或 request\_id 格式不合法。

# 测试已经结束或接口未开放时，连接可能直接失败，没有HTTP响应。不要在测试结束后调用 /exit 查询原因。

# **10. 完整计时示例**

本示例只说明指令含义和虚拟耗时计算，不代表查找策略。

指令序列：

<table><thead><tr><th><p>步骤</p></th><th><p>指令</p></th><th><p>位置参数</p></th><th><p>频道参数含义</p></th><th><p>移动距离(m)</p></th><th><p>移动耗时(s)</p></th><th><p>切换频道耗时(s)</p></th><th><p>检测/清除动作耗时(s)</p></th><th><p>本步总耗时(s)</p></th><th><p>虚拟时刻(s)</p></th></tr></thead><tbody><tr><td><p>1</p></td><td><p>/enter</p></td><td><p>-</p></td><td><p>-</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td></tr><tr><td><p>2</p></td><td><p>/measure</p></td><td><p>(300,400)</p></td><td><p>检测频道 1</p></td><td><p>500</p></td><td><p>100</p></td><td><p>0</p></td><td><p>5</p></td><td><p>105</p></td><td><p>105</p></td></tr><tr><td><p>3</p></td><td><p>/measure</p></td><td><p>(300,400)</p></td><td><p>检测频道 2</p></td><td><p>0</p></td><td><p>0</p></td><td><p>1</p></td><td><p>5</p></td><td><p>6</p></td><td><p>111</p></td></tr><tr><td><p>4</p></td><td><p>/clear</p></td><td><p>(300,0)</p></td><td><p>目标干扰源频道 3</p></td><td><p>400</p></td><td><p>80</p></td><td><p>0</p></td><td><p>3</p></td><td><p>83</p></td><td><p>194</p></td></tr><tr><td><p>5</p></td><td><p>/measure</p></td><td><p>(300,0)</p></td><td><p>检测频道 2</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>5</p></td><td><p>5</p></td><td><p>199</p></td></tr><tr><td><p>6</p></td><td><p>/exit</p></td><td><p>-</p></td><td><p>-</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>0</p></td><td><p>199</p></td></tr></tbody></table>

关键说明：

- 第 2 步从 (0,0) 到 (300,400)，移动距离为 500 米，移动耗时 100 秒；检测频道为 1，与初始频道相同，不需要切换频道。
- 第 3 步位置不变，但检测频道从 1 改为 2，产生 1 秒切换频道耗时。
- 第 4 步是 /clear，频道 3 是目标干扰源频道，不是测向机切换频道；本例假设未发现可清除目标，所以清除动作耗时为 3 秒。
- 第 5 步再次 /measure 频道 2，测向机仍保持第 3 步检测后的频道 2，所以没有切换频道耗时。

# **11. 示例程序**

下面代码只演示如何发送 4 类请求并读取响应，不包含查找策略。

Python版：

<strong>import</strong> json
<strong>from</strong> urllib.request <strong>import</strong> Request, urlopen


<em># 修改为你们自己的参赛队号</em>
BASE\_URL = "http://127.0.0.1:2026"
ROBOT\_ID = "&lt;参赛队号&gt;"


<strong>def</strong> post(path, payload):
    request = Request(
        BASE\_URL + path,
        data=json.dumps(payload).encode("utf-8"),
        headers={"Content-Type": "application/json"},
        method="POST",
    )
    <strong>with</strong> urlopen(request, timeout=5) <strong>as</strong> http\_response:
        response = json.loads(http\_response.read().decode("utf-8"))
    print(path, response)
    <strong>return</strong> response


<strong>def</strong> base(request\_id):
    <strong>return</strong> {
        "arena\_id": "default",
        "robot\_id": ROBOT\_ID,
        "request\_id": request\_id,
    }


<strong>def</strong> action(request\_id, x, y, channel):
    payload = base(request\_id)
    payload["position"] = {"x": x, "y": y}
    payload["channel"] = channel
    <strong>return</strong> payload


<strong>def</strong> main():
    enter\_response = post("/enter", base("enter-1"))
    <strong>if</strong> enter\_response.get("accepted") <strong>is not</strong> True:
        print("进入失败")
        <strong>return</strong>

    remaining\_time = enter\_response["remaining\_real\_duration\_s"]
    print("本局可用现实时间：", remaining\_time, "秒")

    <em># 与第10节计时示例完全相同的四个动作。</em>
    actions = [
        ("/measure", action("measure-1", 300, 400, 1)),
        ("/measure", action("measure-2", 300, 400, 2)),
        ("/clear", action("clear-1", 300, 0, 3)),
        ("/measure", action("measure-3", 300, 0, 2)),
    ]

    <strong>for</strong> path, payload <strong>in</strong> actions:
        <em># 如果因网络故障重试本动作，必须复用这个payload及其中的request\_id。</em>
        response = post(path, payload)
        <strong>if</strong> response.get("accepted") <strong>is not</strong> True:
            print("请求未执行")
            <strong>return

        if</strong> path == "/measure":
            <strong>if</strong> response["measure\_result"] == "direction":
                print("示向度：", response["svd\_deg"], "度")
            <strong>elif</strong> response["measure\_result"] == "near":
                print("距离过近，没有示向度")
            <strong>else</strong>:
                print("未测得信号")
        <strong>else</strong>:
            <strong>if</strong> response["clear\_result"] == "success":
                print("清除成功")
            <strong>else</strong>:
                print("清除位置附近没有目标")

    exit\_response = post("/exit", base("exit-1"))
    <strong>if</strong> exit\_response.get("accepted") <strong>is</strong> True:
        print("退出原因：", exit\_response["exit\_reason"])

main()

Matlab版：

<strong>function</strong> demo\_robot()

<em>% 修改为你们自己的参赛队号。</em>
baseUrl = 'http://127.0.0.1:2026';
robotId = '&lt;参赛队号&gt;';
options = weboptions('MediaType', 'application/json', 'Timeout', 5);

enterRequest = baseRequest(robotId, 'enter-1');
enterResponse = post(baseUrl, '/enter', enterRequest, options);
<strong>if</strong> enterResponse.accepted \~= true
    fprintf('进入失败\n');
    <strong>return</strong>;
<strong>end</strong>

remainingTime = enterResponse.remaining\_real\_duration\_s;
fprintf('本局可用现实时间：%d秒\n', remainingTime);

<em>% 与第10节计时示例完全相同的四个动作。</em>
actions = {
    '/measure', actionRequest(robotId, 'measure-1', 300, 400, 1);
    '/measure', actionRequest(robotId, 'measure-2', 300, 400, 2);
    '/clear', actionRequest(robotId, 'clear-1', 300, 0, 3);
    '/measure', actionRequest(robotId, 'measure-3', 300, 0, 2)
};

<strong>for</strong> i = 1:size(actions, 1)
    path = actions{i, 1};
    payload = actions{i, 2};
    <em>% 如果因网络故障重试本动作，必须复用这个payload及其中的request\_id。</em>
    response = post(baseUrl, path, payload, options);
    <strong>if</strong> response.accepted \~= true
        fprintf('请求未执行\n');
        <strong>return</strong>;
    <strong>end

    if</strong> strcmp(path, '/measure')
        <strong>if</strong> strcmp(response.measure\_result, 'direction')
            fprintf('示向度：%.2f度\n', response.svd\_deg);
        <strong>elseif</strong> strcmp(response.measure\_result, 'near')
            fprintf('距离过近，没有示向度\n');
        <strong>else</strong>
            fprintf('未测得信号\n');
        <strong>end
    else
        if</strong> strcmp(response.clear\_result, 'success')
            fprintf('清除成功\n');
        <strong>else</strong>
            fprintf('清除位置附近没有目标\n');
        <strong>end
    end
end</strong>

exitRequest = baseRequest(robotId, 'exit-1');
exitResponse = post(baseUrl, '/exit', exitRequest, options);
<strong>if</strong> exitResponse.accepted == true
    fprintf('退出原因：%s\n', exitResponse.exit\_reason);
<strong>end
end

function</strong> request = baseRequest(robotId, requestId)
request = struct( ...
    'arena\_id', 'default', ...
    'robot\_id', robotId, ...
    'request\_id', requestId);
<strong>end

function</strong> request = actionRequest(robotId, requestId, x, y, channel)
request = baseRequest(robotId, requestId);
request.position = struct('x', x, 'y', y);
request.channel = channel;
<strong>end

function</strong> response = post(baseUrl, path, payload, options)
response = webwrite([baseUrl path], payload, options);
fprintf('%s  %s\n', path, jsonencode(response));
<strong>end</strong>

# **12. 编程注意事项**

- 先在模拟器中在线登录，启动测试，等待界面显示机器狗接口已经就绪。
- 使用当前登录参赛队号作为 robot\_id。
- 每个新动作使用新的 request\_id；只在重试完全相同动作时复用原ID和原请求内容。
- 逐次等待响应，不得并发发送不同动作；正常串行合法动作没有速率上限。
- 同时检查HTTP状态和 accepted。accepted=false 时动作没有生效，virtual\_time\_s=0 不是当前虚拟时刻。
- 使用 /enter 返回的 remaining\_real\_duration\_s 控制现实运行时间，不要固定假定有1200秒。
- 每次检测增加的5秒是虚拟耗时，现实中不需要等待5秒。
- 只有 measure\_result="direction" 时才读取 svd\_deg，并且不是精确的真实方位，有误差。
- /clear 不切换测向机频道；只有成功的 /measure 可能会更新当前频道。
- no\_signal 不等于附近一定没有干扰源；也可能是超出接收距离或不在定向覆盖范围内。
- 测试超时、中止或其他原因结束测试后接口会关闭；不要尝试调用 /exit 查询原因。
- 程序判断JSON取值时使用 direction、near、no\_signal、success、no\_target\_in\_range 和 user\_exit 等实际字符串，不依赖界面中文说明。
- 机器狗程序应该自行记录测试过程中的指令序列、响应信息等内容，模拟器没有这一功能。
- 倒计时未结束、测试已经结束或接口未开放时，连接可能直接失败，没有JSON体。
- 加密日志文件上传上限为2MB，与机器狗接口协议无关；机器狗程序应避免异常高频循环。