# 固定历史场景聚类对照（开发诊断，首轮有配置错误）

首轮 `ward3_joint_ridge28_30days` 省略了执行器的 `charge_deadband` 参数，实际沿用了 20 kWh 默认值，配置却记录为 0。这使下面首轮结果不能作为场景选择的单因素对照，已排除该轮候选资格并保留全部原始结果和源快照，详见 `correction.json`。

修正轮已以显式 `charge_deadband=0` 完整重跑，独立目录为 `ward3_joint_ridge28_deadband0_30days`。真实费用 **1,267,259.98 元**，比原等距三场景增加 **670.10 元（0.05291%）**；换向159次（原157），AC吞吐1,087,383.87 kWh，活动3278槽，初末SOC与基准相同。物理与结算全部通过，未达预定改善门槛，不扩全年。以下为保留的首轮错误配置历史，不能代替修正轮结论。

采用 Joint CNN + 固定 Ridge28 预测，将过去 28 个完整日的净负荷残差路径按固定的逐槽及累计电量距离进行 Ward 聚类，保留 3 条簇内代表路径及簇样本占比。使用小时模式、50 元换向辅助惩罚、5 秒 MIP 限时及全历史路径 120 次闭环修正。实际费用不包含辅助惩罚。

2025-02-01 至 2025-03-02 的 30 日连续回放：

| 指标 | 原等距三场景 | 加权聚类三场景 |
|---|---:|---:|
| 实际费用（元） | 1,266,589.87 | 1,266,514.46 |
| 非空方向反转 | 157 | 159 |
| AC 充放电吞吐（kWh） | 1,082,838.78 | 1,089,976.91 |
| 活跃时段 | 3,345 | 3,238 |
| 期末 SOC（kWh） | 1,200 | 1,200 |

费用仅下降 75.41 元（0.00595%），未通过预先记录的同区间 0.3% 改善门槛，因此不扩展全年。所有实际物理与分项结算核验通过，场景内同时充放电及紧急电充电均为 0。MIP 最大相对间隙 0.5025%，如实保留限时可行解，不宣称全部达到 0.2% 求解目标。

该比较只针对这一个固定场景缩减实现，不能推断所有聚类方式都无效，也不能与 exp006 全年费用直接比较。四项权重、场景复制等价及物理反例测试通过。
