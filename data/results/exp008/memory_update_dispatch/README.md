# 固定 MemoryForecasts 跨问题联动：30 日后停止

固定候选为 `EnergyMemoryForecasts`：Joint CNN seed42 → 原 Ridge28 → 前一完整日基础负载预测残差的 0.5 倍日能量记忆修正；无额外训练、参数搜索或当日真值选择。模型标识为 `exp008_joint_cnn_seed42_ridge28_load_energy_memory_fixed_gain_half`。

比较期为 2025-02-01 至 03-02，Q3/Q4-3 都沿用原最优 `update_value_diagnostic/future1.5_d334_issued` 的参数：同发布时间历史残差、未来短缺权重 1.5、7 条代表情景、throughput 0.005、variation 0.001、terminal 1、charge deadband 20 kWh、四次法定更新。基准直接独立重算已有年档案的前 30 日，没有重跑或改动基准。每条策略以自己的实际日末 SOC 连续传递。

| 场景 | 基准费用/元 | Memory 费用/元 | 费用变化 | 非空换向 | 吞吐量/kWh | active slots |
|---|---:|---:|---:|---:|---:|---:|
| Q3 | 1218879.98 | 1230277.16 | +11397.19（+0.9351%） | 461 → 515 | 1149235.83 → 1156183.98 | 3637 → 3664 |
| Q4-3 | 1283877.47 | 1293550.78 | +9673.31（+0.7534%） | 511 → 543 | 1235358.44 → 1257401.92 | 3539 → 3683 |

分项费用揭示了代价来源：Q3 日前费用减少 39448.80 元，但上调费用增加 31814.04 元、紧急费用增加 19031.95 元；Q4-3 日前费用减少 43181.74 元，但上调增加 33339.85 元、紧急增加 19515.20 元。下调费用均为 0。新的预测管线降低了日前采购，后续补购费用超过节省；这不是仅凭点预测误差就能断言经济收益的场景。

Q3 初始 SOC 同为 1507.622628752613 kWh，期末为基准 1540.93、Memory 1536.03 kWh；Q4-3 初始 SOC 同为 1522.7387288211346 kWh，期末为基准 1605.51、Memory 1715.46 kWh。两条新策略均无同充放，平衡、SOC、跨日连续性、功率与对原始附件的逐槽核费全部通过。

预先固定的扩展条件是两个场景各自费用与非空方向换向都不恶化，并至少一项严格改善，且物理/因果检查通过。本次两个场景的费用和换向均上升，`extension_gate.json` 为 false，按约定停在 30 日，没有扩展 334 日或再调参数。这两个不同信息场景没有套用 Q2 的 8% 门槛，未作最终模型选择、未生成最终报告。

因果与来源证据：

- `causality_verification.json`：8 组未来扰动，使用被扰动的实际数据重新构建 MemoryStore，当前预测和同发布历史残差严格不变；另外 8 组四发布时间的全部 28 条路径由同一 Memory 管线逐项重构，历史标签均为此前完整日。
- 每个场景的 `completion.json` 固定 override model_id、预测值/origins hash、模型与来源文件 hash；120 个发布审计同时保存当前 load/PV/price hash 与同发布残差 hash，历史来源不再误称旧冻结 CNN。一月周期冷启动仍单独标注。
- Q3/Q4-3 继续使用官方已发布 PV 与同一因果价格预测；动态真实电价仅用于已执行区间结算。执行器逐槽只依赖该槽净余额与已实现 SOC，未使用未来电价/负载。
- `source_archive` 和 `forecast_archive` 保留代码与预测证据；`source_verification.json` 确认运行前后 14 份源码和 8 份预测证据 hash 一致。基础联合网络/Ridge28 训练因果性沿用复制的独立审计，接口扰动检查不冒充重新训练证明。

`summary.json` 汇总全部比较，`comparison.csv` 提供指标平表，`days30/<scenario>/paired_daily.csv` 保留逐日分项。各目录均有新方案 `verification.json`、基准 `baseline_verification.json`、`paired_findings.json`。
