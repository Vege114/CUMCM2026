# 唯一周度重训候选：公平预测门槛未通过

协议在训练前记录。每日午夜仍发行当天144槽预测，只把网络权重从每月更新改为从2月1日起每7天从头训练一次：48个模型、47个7日块及最后5日块，完整覆盖334日。

共享4850参数架构、原对称component+net Huber损失均沿用`neural_joint`，没有采用累计损失候选。每个模型使用asof前最后7个完整日验证，训练issue为day>=7且严格早于验证；均值、两分量尺度和净需求尺度均截止验证开始日之前。seed42、CPU2线程、Adam .001、batch32、最多60epochs、patience6、90日样本权重半衰均不变。神经网络训练合计54.44秒，没有扫描其他重训频率。

对原月度和新周度两套输出分别重新应用同一个固定Ridge28，再应用固定0.5负载日能量记忆。月度重新后处理与已有Ridge28、memory档案逐值一致。

| 预测阶段 | 月度净RMSE / kW | 周度净RMSE / kW |
|---|---:|---:|
| 原始网络 |431.122|432.940|
| 加固定Ridge28 |384.592|390.560|
| 再加固定0.5记忆 |375.585|378.825|

主对照为最后一行完整管线。周度高价净RMSE358.230→362.542 kW，日净能量RMSE3723.897→3860.410 kWh，日净能量MAE2679.639→2778.649 kWh，累计前缀RMSE2351.532→2417.108 kWh。预先记录的8项净/高价/日累计指标全部未通过，因此不运行LP费用桥接、不继续扫描训练频率。6/9/10月的日能量RMSE分别增加314.29/270.65/296.35 kWh；3月和11月略改善，不能据此事后按月选模型。

独立审计核对48份训练/验证/预测边界、全部权重与数组散列、训练尺度重算、Keras中真正保存的损失类型与参数、全部334日拼接以及后处理标签边界。所有保存损失均为原`JointTariffHuber`。每块全预测块的权重重载核验通过；首末预测日合计96次未来特征变异输出差异0。周度memory的PV逐值等于其自身Ridge28 PV。

2月1日及3月1日恰与原月度训练日期相同，这两个新模型的全部权重和优化器状态与原月度模型逐值一致；说明一致条件下复现原训练，正式差异来自之后重训时点。原月度网络、Ridge28和memory档案散列均保持不变。

完整模型位于`runs/d9b0227c7f3e182b/`，包括每个块的`.keras`权重、`.npz`预测/基底/尺度及`.json`训练审计。整体原始、Ridge28、memory预测分别为`raw_predictions.npz`、`ridge28_predictions.npz`、`memory_predictions.npz`；评分为`annual_metrics.csv`、`monthly_metrics.csv`、`daily_metrics.csv`；完整独立核验为`independent_verification.json`，门槛为`prediction_gate.json`。

实现为`experiments/exp008/neural_joint_weekly.py`，独立核验为`neural_joint_weekly_audit.py`。本候选保留为2025开发年中的失败实验，不是独立测试，也没有替代月度Joint+Ridge28+memory主模型。
