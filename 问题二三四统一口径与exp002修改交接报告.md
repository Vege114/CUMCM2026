# 问题二、三、四统一口径与 exp002 修改交接报告

> 交接日期：2026-09-12  
> 面向对象：负责问题二、三、四的队友及其 GPT。  
> 本报告用途：同步主笔已经确定的模型要求，说明 exp002 与问题一的差异，并给出后续修改、重算和验收顺序。  
> 状态声明：本报告是交接要求与分析意见，不代表下述修改已经实现。exp002 现有数值属于旧协议下的实验结果，不得直接改名为统一协议结果。

## 1. 先读结论：哪些已经确定，哪些仍需讨论

主笔已经明确决定以下三条，后续不再把它们当作待选择方案：

1. **电池往返效率为 90%**，充、放电对称分配效率。
2. **问题一的三层混合整数线性规划主线必须贯穿问题二、三、四**。费用表达可以随场景变化，但第二层的运行质量优化、第三层的购电量优化必须保留，不能被一次加权求和或贪心执行替代。
3. **下调购电量只退还下调部分原购电金额的 50%**，不是在原计划全额费用上再加收 50% 违约费。

推荐的总体处理是：**保留 exp002 的预测网络、因果信息处理和实验基础设施，重构统一调度与结算后重算；复杂场景树和风险项先作为可检验的扩展，不因技术复杂就预设其优于基础模型。**

以下内容需要明确设计并说明，不能擅自改变含义：随机情况下第二、第三层的场景聚合方式、计划与执行之间的反馈机制、滚动窗口末端处理、极端误差下运行偏好与安全约束的优先级、风险项是否进入正式第一层。

## 2. 依据文件与阅读边界

以下路径均相对于本项目 `mass` 目录：

| 文件 | 用途 |
|---|---|
| `Xelatex/数模通用模板.tex` | 当前问题一正文、问题分析及完整代码附录，是统一模型的主要依据 |
| `Xelatex/code/q1_reproduce.py` | 问题一实际求解与检验程序，应核对变量、参数和约束的具体实现 |
| `Xelatex/q1_run/summary.json` | 问题一既有计算结果及检验汇总 |
| `reports/experiments/exp002/report.html` | 队友最新交互报告 |
| `reports/experiments/exp002/report.md` | 配套完整文字报告及结果对照 |
| `reports/experiments/exp002/methods.md` | 预测、场景、控制和结算方法 |
| `reports/experiments/exp002/protocol.json` | 冻结实验协议 |
| `reports/experiments/exp002/verification.json` | 既有工作簿与物理核验记录 |

本次交接基于报告、协议、已有核验记录及当前问题一正文的阅读，**没有独立重跑 exp002，也没有完成其实验分支源码的逐行审计**。报告记录的冻结提交为 `ab359643a10d584546f3c397d021f3ae44487a49`；方法目录 `experiments/common/neural_v2/` 在本次阅读所在工作区未找到，应由实验维护方在对应分支核对源码。

## 3. 主笔这边的核心思路：问题一是后续问题的优化基础

### 3.1 为什么采用三层，而不是只追求最低电费

单纯最低费用方案可能存在大量经济上近似等价的解，电池功率却频繁跳变、启停。我们允许在明确、很小的费用预算内调整方案，优先改善电池运行质量，最后再从保留前两层成果的方案中减少购电量。

这不是把三种目标直接相加，而是具有明确优先级的顺序优化：

**费用最小化 → 费用预算内改善电池运行 → 保留运行质量后减少购电量。**

严格名称应为“三层混合整数线性规划”：模型有充放电模式等二元变量，不能笼统宣称为纯连续线性规划。

### 3.2 统一物理与单位

\[
\eta_c=\eta_d=\sqrt{0.9}\approx0.9486832981,
\qquad \eta_c\eta_d=0.9.
\]

以交流侧功率表示充放电时，状态递推为：

\[
E_{t+1}=E_t+\eta_cP_t^{\mathrm{ch}}\Delta t
-\frac{P_t^{\mathrm{dis}}\Delta t}{\eta_d},
\qquad \Delta t=\frac16\ \mathrm{h}.
\]

其中功率单位为 kW，储电量和购电量单位为 kWh。若充放电变量已经是区间电量，状态方程不再乘以时间步长。

沿用储电量范围 1200—10800 kWh、交流侧充放电功率上限 5000 kW，保留充放电互斥。不要把 kW 与 kWh 混用，也不要重复乘除效率。

### 3.3 问题一的三层目标及参数

设统一可行域为 \(\mathcal X\)，问题一中 \(g_t\) 是各区间购电量，\(p_t\) 是单价。

**第一层：**

\[
C^\star=\min_{x\in\mathcal X}F_1(x),
\qquad F_1(x)=\sum_t p_tg_t.
\]

注意：后续费用预算的基准，是**同一运行约束集合下**的第一层最优费用，不是另一个更宽松可行域下的经济最优费用。

**第二层：** 定义功率总变差 \(\mathcal V\)、启动次数 \(N_s\)、短运行段缺额 \(B\)：

\[
\mathcal V=\sum_t|P_t^{\mathrm{bat}}-P_{t-1}^{\mathrm{bat}}|,
\qquad
B=\sum_j(K-\ell_j^{\mathrm{run}})^+.
\]

\[
F_2(x)=\frac{\mathcal V}{2P_{\max}T}
+\frac{N_s}{2T}
+\frac{B}{2T(K-1)}.
\]

\[
S^\star=\min_{x\in\mathcal X}F_2(x),
\quad
F_1(x)\le(1+\delta)C^\star+\epsilon_C.
\]

现有主值为 \(\delta=0.001\)、\(\epsilon_C=10^{-4}\) 元。绝对值通过辅助变量线性化，启停及短段定义按问题一程序实现。指标是运行质量代理，**不是已标定的电池寿命损失或退化成本**。

**第三层：**

\[
\begin{aligned}
\min_{x\in\mathcal X}\quad &F_3(x)=\sum_tg_t,\\
\mathrm{s.t.}\quad
&F_1(x)\le(1+\delta)C^\star+\epsilon_C,\\
&F_2(x)\le S^\star+\epsilon_S,
\qquad\epsilon_S=10^{-7}.
\end{aligned}
\]

问题一已经选定的运行设置包括：每十分钟净电池功率变化上限 1000 kW，同模式最短运行 3 段、最短关闭 2 段，短段参照长度 \(K=3\)。这些是已选运行偏好，不是题目给出的硬件定律；后续应继承并进行适配检验，不能默默删除或擅自改值。由于主方案 \(U=K=3\)，其中短段缺额为零，保留该指标便于统一比较，不应虚构它在主方案中仍有额外改善。

### 3.4 已有结果用于说明取舍，不能移植为后续结果

问题一改进方案费用约 **34115.03 元**，购电量约 **57560.62 kWh**，相较原始经济基准功率总变差下降约 **67.66%**。

费用口径必须区分：相较原始经济基准，总费用增加约 **0.928%**；**0.1% 仅是新增运行约束下的第一层费用放宽比例**，不是相对原始经济基准的全部代价。

### 3.5 “全部继承”与跨日边界的正确衔接

问题一典型日采用日初日末 6000 kWh 相等及循环运行边界。当前正文已明确：后续随机场景按真实储电量跨日传递。因此，不应机械地把每一天重置为 6000 kWh，也不能将独立典型日的循环索引直接用于每一个滚动窗口。

后续应继承的是物理模型、三层优先级及运行要求，并正确传递：

- 实际储电量；
- 上一个区间的电池功率；
- 当前充、放电模式；
- 已连续运行或关闭的时长；
- 尚未履行完毕的最短运行、关闭承诺。

滚动窗口末端必须防止通过截断窗口逃避运行约束。终端储能目标或价值如需新增，应说明依据和影响，不能在没有说明的情况下引入或撤销。

## 4. 下调结算：必须修改模型和回放，不只是修改文字

设 \(g_t^0\) 为原申报购电量，\(g_t\) 为最终执行前计划，\(e_t\) 为紧急购电量，\((a)^+=\max(a,0)\)。暂沿用 exp002 的上调和紧急购电价格，仅修正主笔已经确定的下调项：

\[
C=\sum_t\left[
p_tg_t^0
+1.5p_t(g_t-g_t^0)^+
-0.5p_t(g_t^0-g_t)^+
+5p_te_t
\right].
\]

**错误旧式**是在原计划全额费用基础上写成 \(+0.5p_t(g_t^0-g_t)^+\)。正确新式是退款，必须是负号。

例：原计划 100 kWh，最终 80 kWh，单价 1 元/kWh，无紧急购电。正确费用为 90 元，不是 110 元。

同时注意等价表示：仅下调时，\(p_tg_t+0.5p_t(g_t^0-g_t)\) 同样等于 90 元，这里的正项是未退还的原款。判断是否重复收费必须看基数，不能只看“加上 50%”字样。

实现要求：

1. 优化目标、场景费用、验证回放、独立核算、工作簿与报告必须共用一致规则。
2. 原申报量、最终调整量和紧急购电量应分别留存；中间修订是否只按最终净调整计费，继续核对题面并明确记录。
3. 问题四优化只能使用当时可获得的电价信息，事后实测价格用于结算；原计划及调整适用的价格时点需明确，不要让符号简写掩盖时点差异。
4. 若以非负上调量、下调量线性化，应保证与净调整定义等价，并验证不存在同时虚增或套利。
5. 增加不调整、仅上调、仅下调、多次修订后净上调／净下调、紧急购电组合的手算单元测试。
6. 重新求解和回放，不能只对旧轨迹更换费用符号后称为新最优方案。旧轨迹重新计费可以作为诊断，但必须单独标注。

## 5. exp002 已完成的进度及可保留成果

### 5.1 评价范围和交付状态

正式评价期为 2025-02-01 至 2025-12-31，共 **334 天**、每日 144 个区间；1 月用于预热、训练和校准。正文应写“评价期总费用”，不能不加说明地称为完整 365 天全年费用。

已完成 33 组训练和回放，正式使用种子 42，种子 2026、3407 用于稳定性检验；四个工作簿及配套分析已经交付。已有核验记录显示其所检查的物理约束无违规，最大能量残差约 \(4.55\times10^{-13}\) kWh。此结果不意味着满足了问题一新增的全部运行约束。

### 5.2 预测与信息处理建议保留

- 四个独立的轻量残差网络分支，结构为 16→32→16→1，总参数 4356。
- 周期基础预测加网络修正：负载、电价使用上周同期基础，历史光伏使用昨日同期基础，预报修正使用当时已发布附件预报。
- 月度训练、过去七天验证、跨界标签剔除、当月冻结检查点，避免未来信息泄露。
- 光伏小时目标点修正后进行分段线性积分，统一十分钟区间功率、电量语义。
- 问题二、四—二不读取附件三；问题三、四—三仅使用已经发布的版本。
- 联合误差路径、信息树、价格信息隔离和非负裁剪后可观测信息检查。
- 费用拆分、预测与调度贡献分离、种子稳定性、失败日期定位及独立核验框架。

报告中的正式 WAPE 为：负载 3.5449%、电价 5.4940%、历史光伏 7.2505%、光伏预报修正 6.6501%。这些是预测成绩，不等同于调度费用的改进幅度。

训练由旧版 165 组降至 33 组，累计训练时间由约 98.36 分钟降至 11.26 分钟。不要将训练节省误写成全流程同比节省。

## 6. exp002 与统一模型的差异清单

| 项目 | 当前 exp002 | 统一要求或处理 |
|---|---|---|
| 效率 | 两侧均为 √0.9 | 已一致，保留 |
| 第一层 | 场景期望费用＋λCVaR | 可作为扩展，但先修正结算，明确正式目标 |
| 第二层 | 未见问题一运行质量的顺序优化 | 必须补齐 |
| 第三层 | 未见保留前两层后的购电量最小化 | 必须补齐 |
| “三层核心对照” | 旧预测基础调度／新预测基础调度／新预测风险调度 | 这是三组实验，不是三层优化，应更名避免混淆 |
| 储能动作 | 当前净盈余决定尽量充电或放电 | 不等于继承问题一，需衔接受约束的因果控制 |
| 运行约束核验 | 能量、功率、效率、互斥、状态等 | 增加爬坡、启停、最短运行关闭、窗口及跨日边界 |
| 下调计费 | 原计划全额＋下调额外收费 | 改为退还下调部分原金额的 50% |
| 日末状态 | 连续传递、仅物理边界 | 不每日重置；补齐其他运行状态传递 |
| 求解质量 | 两秒限时可行解，几乎全部超时 | 不得宣称全局最优，应调整预算并报告证书 |

### 6.1 因果执行不等于必须采用贪心充放电

exp002 当前规则是：有净盈余尽量充电，净不足尽量放电，电池不足再紧急购电。这能防止场景内电池利用未来信息，却不自然保证功率平滑与最短运行时间。净盈余连续正、负、正时，仍可能产生频繁充放电切换。

应保留“不偷看未来”这一原则，而非把贪心控制器当成唯一实现。三层滚动优化也可以严格因果：只使用当时已知数据、冻结已执行动作，按当前实际状态求解可实施控制，并把状态传递到后续时刻。

必须同时说明：问题二、四—二的固定购电计划不能因控制反馈而被擅自修改；电池如何实时响应、何时允许紧急购电，与问题三、四—三可更新购电的权限要分别处理。

### 6.2 随机条件下不能只让计划曲线平滑

需要区分预测计划、场景控制和实际执行三种轨迹。若只优化预测轨迹，执行却切回无运行约束的贪心规则，不能宣称已经实现三层平稳调度。

若扩展为场景模型：第二层对各场景运行质量如何聚合、第三层购电是否包含紧急购电、共享信息节点上的控制变量如何约束，都应给出清楚定义。建议先建立确定性预测输入下的统一三层基线，再增加场景扩展，避免一次修改过多导致无法归因。

突发预测误差下，紧急购电、弃电等应与电池运行约束共同保证可行性。对功率突变、连续运行约束冲突等边界案例进行测试；如必须设置运行偏好的应急松弛，应单列条件、优先级、次数和代价，并提交主笔确认，不能默默放松。

## 7. 已有数值告诉我们什么，不能告诉我们什么

下表全部属于 **exp002 当前旧结算及执行协议**，只用作诊断与归档。

| 场景 | 新预测＋基础调度总费／元 | 正式场景调度总费／元 | 后者相对变化 |
|---|---:|---:|---:|
| 问题二 | 15,352,302.2799 | 15,128,897.4527 | -1.455% |
| 问题三 | 13,257,848.3582 | 14,005,062.7759 | +5.636% |
| 问题四—二 | 16,114,809.6566 | 15,901,397.2011 | -1.324% |
| 问题四—三 | 14,026,450.8280 | 14,734,925.1249 | +5.051% |

由此可见，预测成果值得保留，但复杂场景调度没有全面优于基础调度。问题三、四—三的日费用 CVaR90 也更高，不能解释为“增加平均费用换取尾部风险降低”。

问题三相较同预测基础调度，紧急购电费增加约 898550.43 元，总费净增加约 747214.42 元；问题四—三紧急购电费增加约 881288.70 元，总费净增加约 708474.30 元。可以确认费用恶化发生在哪个账目，**不能唯一归因于信息树、场景近似或求解时限中的某一项**。

### 7.1 CVaR 的实际作用应据实描述

报告采用期望费用加 CVaR90，风险权重一月校准后：问题二为 0.1，其余三个场景均为 0。权重为零时仍是场景期望费用模型，但不能宣称非零尾部惩罚发挥了作用。日费用的事后 CVaR 与单次优化的场景 CVaR 也不能混为同一指标。

如正式第一层采用“期望费用＋风险项”，则后续 0.1% 放宽默认约束的是该复合目标，不自动等价于实际费用仅增长 0.1%。要么明确分开费用与风险约束，要么准确说明预算语义，提交主笔确认。

### 7.2 求解质量是当前实质性限制

正式种子合计 3340 次风险求解，3339 次超时，0 次取得不超过 1% 的全局差距证书，0 次真正回退。原因是可以采用先得到的区域 LP 可行候选，故“超时”与“没有回退”可以同时成立。

可行解不等于最优解。当前实验能说明限时策略的实际表现，不能证明复杂模型在充分求解后仍然更差或更好。

尤其三层求解中，若第一层未证明最优，不能把可行上界直接称为 \(C^\star\)，继而宣称有严格的近最优费用保证。必须记录上下界、求解差距和状态，准确标注相对 incumbent 的预算，或者提高预算取得所需证书。

## 8. 建议的修改与重算顺序

### 第一步：冻结旧成果，统一协议

- 保留 exp002 原报告、结果、配置及源码签名，作为旧协议档案。
- 建议采用新实验编号或明确的新协议版本，不覆盖后伪装为原结果。
- 先落实效率、退款、三层结构、时间与单位、信息权限和运行参数。
- 对需主笔确认的设计选择单独列出，不借实现便利改变确定要求。

### 第二步：先通过小实例与边界测试

- 手算结算样例与代码结果一致。
- 完美预测的小规模场景下，核对三层模型与问题一一致边界设置时的表现。
- 检查跨日、跨更新时点的功率和运行时长连续性。
- 检查储能上下界、突发负载增加、光伏骤降、长时间盈余和短时符号切换。
- 对可穷举的小问题比较目标值和非预见性，避免依靠全年汇总掩盖错误。

### 第三步：完成统一三层基线

- 复用已冻结的新预测，接入三层调度，不必因为结算变化而盲目重训预测网络。
- 问题二、四—二保持购电更新权限不变；问题三、四—三在允许时刻对剩余区间完整执行三层求解。
- 费用预算只在清楚定义的目标与基准上使用，不能每次滚动都在上一次放宽结果上机械再加 0.1%。区分每次局部预算与累计评价期费用。
- 检验计划与实际执行，并记录每层状态、目标值、约束残差和实际运行指标。

### 第四步：再测试复杂扩展是否值得保留

以统一三层基线为参照，分别增加联合误差路径、更新信息树和风险项。测试不同场景数与求解预算，避免只在几乎全部超时的两秒设置下作最终判断。

更长预算是诊断和充分求解的手段，不要求预设具体时间；应综合实际时限、计算能力和证书质量选定。所有对照保留同一数据期、初始状态、执行规则及结算口径。

### 第五步：重新校准、评价和交付

修改结算与调度后，重新完成风险参数校准和调度回放，不能沿用旧目标下选出的风险权重作为已验证最优值。

以问题二、三、四—二、四—三分别汇总：预测成绩、总费用、紧急购电、运行平稳性、求解质量及边界违规。明确旧预测／新预测的贡献与基础／扩展调度的贡献，避免将多项修改合并为一个无法解释的收益数字。

## 9. 下一版的验收清单

- [ ] 四个场景统一使用往返效率 90%，单位和状态方程一致。
- [ ] 下调退款的手算例、优化、执行、导出、报告全部一致。
- [ ] 每次调度真实完成三层顺序求解，或明确记录未完成层及降级情况，不能伪称完成。
- [ ] 第二层保留总变差、启停与短段指标，第三层清楚定义购电量统计范围。
- [ ] 运行参数继承问题一；任何必要变更有说明、对照与主笔确认。
- [ ] 实际轨迹核验覆盖功率变化、最短运行／关闭、跨日和滚动窗口衔接。
- [ ] 所有预测、场景和控制符合对应问题的信息权限；不存在未来数据泄露。
- [ ] 每层报告目标值、状态、耗时、上下界／差距；未知证书保持未知，不填零。
- [ ] 费用分项与总费用一致，重复工作表中的同一天总费不重复累加。
- [ ] 明确评价期为 334 天，不把训练／预热月份隐含计入评价。
- [ ] 在同一新协议下比较基础三层与复杂扩展，保留不利结果。
- [ ] 新结果与旧 exp002 分开归档，报告写清变更、重算范围和缓存复用依据。

建议汇总指标至少包括：总费用及分项、计划购电和紧急购电、弃电、功率总变差、启动次数、短段缺额、最大功率变化、运行时长违规数、能量残差、期末储能、各层超时率与差距证书。不能仅以费用或“总违规数为零”取代完整核验。

## 10. 请队友 GPT 首先反馈什么

请先阅读问题一现有正文与程序，并针对本交接报告给出：

1. 已确认可以直接保留的预测和数据模块。
2. 当前源码中退款公式、控制器及优化入口的具体位置。
3. 将第二、第三层接入问题二、三、四的变量、约束及执行方案。
4. 仍需主笔确认的设计选择，尤其随机聚合、终端处理和应急松弛；不要重新询问已经确定的三条原则。
5. 小实例验证结果、预计重算范围及新旧缓存隔离方式，再进入大规模回放。

最终目标不是证明 exp002 的复杂模型一定优秀，而是形成一套**从问题一到问题四逻辑一致、规则正确、可实施、可复核，且能据实解释收益与代价**的完整论文模型。
