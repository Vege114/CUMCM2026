# 模拟器

用户提供的 `../jammers-simulator.exe` 保留原路径并纳入版本管理，便于小组使用同一文件；SHA-256 见 `../docs/conversion.md`。
本次初始化未运行它。后续需人工登录、选择题目和演练/正式模式，待接口就绪后再运行自己的程序。

说明文档见 `../docs/markdown/附件1.md` 和 `../docs/markdown/附件2.md`。
模拟器生成的 `JammersSimulatorData/`、数据库和登录状态不提交；正式导出日志另存 `submission/logs/` 并保留原文件名。
